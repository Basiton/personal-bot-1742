# 🚀 Быстрая шпаргалка - YandexGPT

## 📋 Быстрый старт (3 шага)

```bash
# 1. Установка
./setup_yandex_gpt.sh

# 2. Настройка .env
nano .env  # добавьте YANDEX_API_KEY и YANDEX_FOLDER_ID

# 3. Тест
python test_yandex_gpt.py
```

## 🔑 Получить API ключ

1. https://console.cloud.yandex.ru/
2. Сервисный аккаунт → API-ключ
3. Скопировать ключ и Folder ID

## 📁 Файл .env

```env
YANDEX_API_KEY=[YOUR_API_KEY_HERE]
YANDEX_FOLDER_ID=b1g4or5i5s66hklqfg06
```

## ✅ Проверка работы

```bash
# Быстрая проверка
python test_yandex_gpt.py

# Проверка конфигурации
python -c "import os; from dotenv import load_dotenv; load_dotenv(); print(os.getenv('YANDEX_API_KEY')[:10])"
```

## 🔧 Основные команды

| Команда | Описание |
|---------|----------|
| `./setup_yandex_gpt.sh` | Установка зависимостей |
| `python test_yandex_gpt.py` | Тест YandexGPT |
| `python main.py` | Запуск бота |

## 📖 Документация

| Файл | Что там |
|------|---------|
| `INTEGRATION_SUMMARY.md` | Подробное описание всех изменений |
| `YANDEX_GPT_GUIDE.md` | Полная инструкция по настройке |
| `.env.example` | Пример конфигурации |

## 🐛 Решение проблем

| Проблема | Решение |
|----------|---------|
| "API key not configured" | Проверьте .env файл |
| "API error: 401" | Неверный API ключ |
| "API error: 429" | Превышен лимит запросов |
| Fallback comments | API не настроен или не работает |

## 💡 Как работает

```python
# Старый способ (шаблоны)
comment = random.choice(templates)

# Новый способ (AI)
comment = generate_neuro_comment(
    post_text="текст поста",
    channel_theme="тема канала"
)
```

## 🔐 Безопасность

✅ API ключ в .env (не в коде)
✅ .env в .gitignore
✅ Не коммитится в git

## 🎯 Что изменилось

- ✅ `import requests` добавлен
- ✅ `python-dotenv` для .env
- ✅ Функция `generate_neuro_comment()`
- ✅ AI комментарии вместо шаблонов
- ✅ Fallback на шаблоны при ошибках

## ⚙️ Настройка параметров

В функции `generate_neuro_comment()`:
- `temperature` - креативность (0.0-1.0)
- `max_tokens` - длина (120 по умолчанию)
- Промпт - в строке `prompt = f"..."`

## 📞 Поддержка

Проблемы? Смотрите:
1. Логи бота
2. `YANDEX_GPT_GUIDE.md`
3. Yandex Cloud Console (баланс, лимиты)

---

**Важно:** Без API ключа бот работает на шаблонах (как раньше)
