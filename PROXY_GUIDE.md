# 🌐 Руководство по использованию прокси

## ⚠️ ВАЖНО: Поддержка прокси в Telethon

### Какие прокси поддерживаются?

**✅ SOCKS5** - полная поддержка "из коробки"  
**❌ HTTP/HTTPS** - НЕ поддерживаются напрямую (требуется дополнительная настройка с `python-socks`)

---

## 📝 Формат прокси-строки

Telethon ожидает кортеж из **6 элементов**:
```python
(proxy_type, host, port, rdns, username, password)
```

### Форматы команды `/auth`

#### Полный формат (6 элементов):
```
/auth +12539061764 socks5:195.69.162.112:64161:True:rxt2BvyE:NZccstPg
```

#### Короткий формат (5 элементов, rdns=True по умолчанию):
```
/auth +12539061764 socks5:195.69.162.112:64161:rxt2BvyE:NZccstPg
```

---

## 🔧 Примеры использования

### SOCKS5 прокси С авторизацией:

```bash
/auth +12539061764 socks5:195.69.162.112:64161:rxt2BvyE:NZccstPg
```

### SOCKS5 прокси БЕЗ авторизации:

```bash
/auth +79123456789 socks5:192.168.1.1:1080:True::
```
(два двоеточия в конце для пустых username/password)

---

## 🌍 Переменные окружения

Для автозагрузки аккаунтов с прокси используйте:

```bash
export ACCOUNT_1_PHONE="+12539061764"
export ACCOUNT_1_SESSION="ваша_сессия"
export ACCOUNT_1_PROXY="socks5:195.69.162.112:64161:rxt2BvyE:NZccstPg"
```

Формат поддерживает:
- **6 элементов**: `socks5:host:port:rdns:user:pass`
- **5 элементов**: `socks5:host:port:user:pass` (rdns=True автоматически)

---

## 🚫 Что НЕ работает

### ❌ HTTP прокси:
```bash
# ЭТО НЕ БУДЕТ РАБОТАТЬ:
/auth +12539061764 http://rxt2BvyE:NZccstPg@195.69.162.112:64161
/auth +12539061764 http:195.69.162.112:64161:rxt2BvyE:NZccstPg
```

**Причина**: Telethon не поддерживает HTTP прокси без дополнительных библиотек.

**Решение**: Используйте SOCKS5 прокси или настройте конвертацию HTTP → SOCKS5 через внешние инструменты (например, `privoxy`).

---

## 📊 Параметры прокси

| Параметр | Описание | Значение |
|----------|----------|----------|
| `proxy_type` | Тип прокси | `socks5` (только это работает) |
| `host` | IP-адрес или домен | `195.69.162.112` |
| `port` | Порт | `64161` (целое число) |
| `rdns` | Удаленное разрешение DNS | `True` или `False` (рекомендуется `True`) |
| `username` | Логин (если нужен) | `rxt2BvyE` или пусто |
| `password` | Пароль (если нужен) | `NZccstPg` или пусто |

---

## 🔍 Проверка прокси

После авторизации прокси сохраняется в `bot_data.json`:

```json
{
  "+12539061764": {
    "session": "...",
    "proxy": ["socks5", "195.69.162.112", 64161, true, "rxt2BvyE", "NZccstPg"],
    "active": true
  }
}
```

---

## 💡 Советы

1. **Всегда используйте SOCKS5** - это единственный поддерживаемый тип
2. **rdns=True** - для большей анонимности (DNS запросы через прокси)
3. **Проверьте прокси** перед использованием (доступность и работоспособность)
4. **Один прокси = один аккаунт** - не используйте один прокси для всех аккаунтов

---

## 🆘 Частые проблемы

### "Connection timeout" или "Proxy connection failed"
- Проверьте доступность прокси
- Убедитесь, что порт правильный
- Проверьте, что прокси поддерживает SOCKS5

### "Authentication failed"
- Проверьте логин/пароль
- Убедитесь, что прокси требует авторизацию

### "Invalid proxy format"
- Проверьте количество элементов (должно быть 5 или 6)
- Убедитесь, что используете двоеточие `:` как разделитель
- Порт должен быть числом

---

## 📚 Дополнительная информация

Документация Telethon по прокси:
https://docs.telethon.dev/en/stable/concepts/botapi-vs-mtproto.html#proxies

---

**Последнее обновление**: 14.01.2026
