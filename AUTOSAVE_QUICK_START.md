# Краткая инструкция: Автосохранение настроек

## 🎉 Что изменилось

Теперь **все настройки бота автоматически сохраняются** и восстанавливаются после перезапуска. Вам больше **не нужно ничего настраивать руками** после `git pull` и `systemctl restart`.

## 📁 Новые файлы

- `config.json` — все настройки бота (автоматически создаётся)
- `config.json.bak` — автоматический бэкап при каждом изменении
- `config_manager.py` — модуль управления конфигурацией

## ✅ Что автоматически сохраняется

Все изменения через команды бота **автоматически сохраняются** в `config.json`:

### Скорость и параллельность
- `/setratelimit 25` → сохраняется скорость
- `/setparallel 3` → сохраняется количество аккаунтов
- `/setrotation 21600` → сохраняется интервал ротации

### Режим работы
- `/setworkermode distributed` → сохраняется режим
- `/setmaxcycles 5` → сохраняется лимит циклов
- `/togglerecovery` → сохраняется автовосстановление

### Тестовый режим
- `/testmode on @channel` → сохраняются тест-каналы
- `/testmode speed 15` → сохраняется скорость теста
- `/testmode off` → отключение сохраняется

### Аккаунты
- `/toggleaccount +1234` → активные аккаунты сохраняются автоматически

## 🔍 Новая команда

```bash
/config
```

Показывает **все текущие настройки** в одном месте:
- Скорость, параллельность, ротация
- Режим воркеров
- Тестовый режим
- Статистика аккаунтов
- Информация о файлах

## 🚀 Типичный сценарий

### Первая настройка (один раз):

```bash
/setratelimit 20              # Скорость
/setparallel 2                # Параллельность
/setworkermode distributed    # Режим для 100+ каналов
/auth +1234567890             # Авторизация аккаунтов
/toggleaccount +1234567890    # Активация
/addchannel @channel          # Добавление каналов
/startmon                     # Запуск
```

### После git pull и перезапуска:

```bash
git pull
systemctl restart your-bot
```

**Всё!** Бот автоматически:
- ✅ Восстанавливает скорость и параллельность
- ✅ Активирует те же аккаунты
- ✅ Применяет тот же режим работы
- ✅ Сохраняет тестовый режим (если был включен)

## 🔒 Безопасность

### Что сохраняется на диске:
1. **Настройки** — в `config.json`
2. **Сессии аккаунтов** — в `bot_data.json` (как и раньше)
3. **Сессия бота** — в `bot_session.session` (как и раньше)

### Важно:
**НЕ УДАЛЯЙТЕ** эти файлы без бэкапа:
- `config.json` — все настройки
- `bot_data.json` — сессии аккаунтов
- `bot_session.session` — авторизация бота

## 📊 Проверка

Команда `/config` покажет все настройки:

```
⚙️ ТЕКУЩАЯ КОНФИГУРАЦИЯ БОТА:

📊 ОСНОВНЫЕ НАСТРОЙКИ:
  • Скорость: 20 комм/час
  • Параллельность: 2 аккаунтов
  • Ротация: 14400 сек (4ч)

🔄 РЕЖИМ ВОРКЕРОВ:
  • Режим: distributed
  • Макс циклов: 3
  • Автовосстановление: ✅ Вкл

👥 АККАУНТЫ:
  • Активных: 2 / 2
  • Всего: 5
```

## 🎯 Главное

**После любого перезапуска бот продолжит работу с теми же настройками. Ручное редактирование файлов НЕ требуется!**

Полная документация: [CONFIG_PERSISTENCE_COMPLETE.md](CONFIG_PERSISTENCE_COMPLETE.md)
