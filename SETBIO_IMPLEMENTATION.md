# ✅ /setbio - Максимально надёжная реализация

## 🎯 Что улучшено

### 1. Детальная обработка ошибок Telegram

**Конкретные ответы для каждого типа ошибки:**

| Ошибка | Что говорим пользователю | Что делаем |
|--------|-------------------------|------------|
| `ABOUT_TOO_LONG` | "Био слишком длинное: N символов. Макс: 70" | Показываем лимит, просим сократить |
| `FROZEN` / `USER_DEACTIVATED` | "Аккаунт заморожен/деактивирован Telegram" | Предлагаем другой аккаунт |
| `FLOOD_WAIT_X` | "Флуд-контроль: подождите X минут" | Извлекаем точное время ожидания |
| `AUTH_KEY_UNREGISTERED` | "Сессия недействительна, нужна переавторизация" | Указываем на `/auth` |
| `PHONE_NUMBER_BANNED` | "Номер забанен в Telegram" | Рекомендуем пометить BROKEN |
| Неизвестная | Полная информация с типом и сообщением | Логируем всё в detail |

### 2. Честная верификация результата

**До:**
```python
result = await client(UpdateProfileRequest(about=new_bio))
# Считаем что всё OK
await event.respond("✅ Био обновлено")
```

**После:**
```python
result = await client(UpdateProfileRequest(about=new_bio))

# ПРОВЕРЯЕМ что реально изменилось!
await asyncio.sleep(0.3)
full_after = await client(GetFullUserRequest(me))
actual_bio = full_after.full_user.about or ''

if actual_bio == new_bio:
    await event.respond("✅ Био обновлено и ПРОВЕРЕНО")
else:
    await event.respond(
        f"⚠️ API вернул успех, но био НЕ изменилось\n"
        f"Ожидали: {new_bio}\n"
        f"Получили: {actual_bio}"
    )
```

### 3. Детальное логирование

**Каждый вызов /setbio логирует:**
```python
logger.info(f"PROFILE UPDATE: cmd=/setbio, phone={phone}")
logger.info(f"   status={status}, admin_id={admin_id}")
logger.info(f"   bio_length={len(new_bio)}")
logger.info(f"   session={bool(has_session)}")

# При успехе:
logger.info(f"SUCCESS - Bio VERIFIED changed")
logger.info(f"   Old: '{old_bio[:50]}'")
logger.info(f"   New: '{new_bio[:50]}'")

# При ошибке:
logger.error(f"ERROR Type: {error_type}")
logger.error(f"ERROR Message: {error_msg}")
logger.error(f"ERROR Traceback:\n{traceback}")
```

---

## 🧪 Диагностический скрипт

### Запуск
```bash
cd /workspaces/personal-bot-1742
python3 diagnose_setbio.py
```

### Что делает скрипт

1. **Загружает все аккаунты** из `bot_data.json`

2. **Для каждого аккаунта:**
   - Проверяет наличие сессии
   - Проверяет авторизацию
   - Получает текущее био
   - Тестирует 3 варианта био:
     - Короткое: "Test bio 1"
     - Среднее с emoji: "Investor | Trader | Crypto enthusiast 🚀"
     - Длинное (68 символов): "Bitcoin maximalist. HODL forever. Not financial advice."
   - Проверяет что био действительно изменилось
   - Восстанавливает оригинальное био

3. **Генерирует MD-отчёт** с:
   - Таблицей результатов всех аккаунтов
   - Детальным описанием каждого теста
   - Конкретными ошибками и рекомендациями
   - Статистикой работающих/сломанных аккаунтов

### Пример отчёта

```markdown
# 🔍 Отчёт о проверке /setbio для всех аккаунтов

**Дата:** 2026-01-17 15:30:00
**Протестировано аккаунтов:** 5

## 📊 Статистика
- ✅ **Работающих:** 3
- ❌ **Сломанных:** 1
- 🔑 **Требуют авторизации:** 1

## 📋 Детальные результаты

| Аккаунт | Статус | /setbio | Ошибки | Рекомендации |
|---------|--------|---------|--------|--------------|
| `...3456789` | ✅ | ✅ Работает | - | - |
| `...9876543` | 🔵 | ✅ Работает | - | - |
| `...1112131` | 🔴 | ❌ Не работает | FROZEN | Ждать или другой аккаунт |
```

---

## 🛡️ Защита от лимитов

### Текущая защита (уже реализована)

**Функция:** `can_do_profile_operation(phone, operation_type)`

**Лимиты:**
```python
RATE_LIMITS = {
    'bio': timedelta(hours=1),      # 1 час между изменениями
    'name': timedelta(hours=1),     # 1 час
    'avatar': timedelta(hours=24),  # 1 день
}
```

**Как работает:**
1. Проверяет время последнего изменения профиля
2. Если прошло меньше лимита → отказ с точным временем ожидания
3. Если аккаунт в `FROZEN_ACCOUNTS` → отказ с объяснением
4. Логирует каждое изменение в БД

**Пользователю показывается:**
```
⏰ Слишком частые операции!

Аккаунт +79123456789 использовался недавно.
Подождите: 45 минут

⚠️ Это защищает аккаунт от блокировки Telegram.

Лимиты:
• BIO: не чаще 1 раза в час
• NAME: не чаще 1 раза в час
• AVATAR: не чаще 1 раза в день
```

---

## 📝 Примеры ответов /setbio

### ✅ Успешное изменение
```
✅ Био обновлено для +79123456789

Было: Crypto trader
Стало: Investor | Trader | Crypto enthusiast 🚀

✅ Изменение подтверждено в Telegram
```

### ⚠️ API OK, но био не изменилось
```
⚠️ API вернул успех, но био НЕ изменилось для +79123456789

Telegram принял запрос, но профиль остался без изменений.

Ожидали: New bio text
Получили: Old bio text

💡 Возможно аккаунт имеет скрытые ограничения.
Попробуйте другой аккаунт.
```

### ❌ Био слишком длинное
```
❌ Био слишком длинное для +79123456789

📏 Длина: 85 символов
⚠️ Telegram: максимум 70 символов

💡 Сократите текст и попробуйте снова
```

### ⏰ Флуд-контроль
```
⏰ Флуд-контроль Telegram для +79123456789

⚠️ Слишком частые изменения профиля
⏳ Подождите: 25 минут (1500 сек)

💡 Это ограничение самого Telegram, не бота
```

### 🚫 Аккаунт заморожен
```
❌ Аккаунт +79123456789 заморожен/деактивирован

⚠️ Telegram полностью ограничил этот аккаунт
🚫 Изменение профиля невозможно

💡 Используйте другой активный аккаунт
```

### 🔑 Сессия недействительна
```
❌ Сессия +79123456789 недействительна

🔑 Аккаунт разлогинен в Telegram
⚠️ Требуется повторная авторизация

💡 Используйте /auth +79123456789 для входа заново
```

### ⛔ Номер забанен
```
🚫 Аккаунт +79123456789 забанен в Telegram

⛔ Номер заблокирован на уровне Telegram
❌ Использование невозможно

💡 Этот аккаунт нужно пометить как broken
```

---

## 🔧 Техническая реализация

### UpdateProfileRequest с about

**Правильный вызов:**
```python
from telethon import functions

# Создаём ПРАВИЛЬНЫЙ клиент для КОНКРЕТНОГО аккаунта
client = TelegramClient(
    StringSession(account_data['session']),  # Сессия ЭТОГО аккаунта
    API_ID,
    API_HASH,
    proxy=account_data.get('proxy')  # Прокси ЭТОГО аккаунта
)

await client.connect()

# Обновляем bio
result = await client(functions.account.UpdateProfileRequest(
    about=new_bio  # ТОЛЬКО about, не first_name/last_name
))
```

**Неправильно:**
```python
# ❌ Использование глобального клиента
await self.bot_client(UpdateProfileRequest(...))  # Это клиент БОТА!

# ❌ Перепутанные клиенты
client = self.some_other_client  # Клиент другого аккаунта!

# ❌ Без проверки результата
result = await client(UpdateProfileRequest(about=new_bio))
# Считаем что всё OK - НЕТ ГАРАНТИИ!
```

### Верификация результата

**Обязательная проверка:**
```python
# Отправили запрос
result = await client(UpdateProfileRequest(about=new_bio))

# Ждём синхронизации
await asyncio.sleep(0.3)

# ПРОВЕРЯЕМ реальный профиль
full = await client(functions.users.GetFullUserRequest(me))
actual_bio = full.full_user.about or ''

# Сравниваем
if actual_bio == new_bio:
    # Действительно изменилось!
    return True, "Verified"
else:
    # API солгал!
    return False, f"Expected '{new_bio}' but got '{actual_bio}'"
```

---

## 📊 Структура базы данных

### Таблица profile_changes

```sql
CREATE TABLE IF NOT EXISTS profile_changes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    phone TEXT NOT NULL,
    change_type TEXT NOT NULL,  -- 'bio', 'name', 'avatar'
    old_value TEXT,
    new_value TEXT,
    change_date TEXT NOT NULL,
    success INTEGER NOT NULL  -- 1 или 0
)
```

**Запрос статистики:**
```sql
-- Успешные изменения bio за последний месяц
SELECT phone, COUNT(*) as changes
FROM profile_changes
WHERE change_type = 'bio' 
  AND success = 1
  AND change_date > datetime('now', '-30 days')
GROUP BY phone;

-- Аккаунты с ошибками
SELECT phone, COUNT(*) as errors, MAX(change_date) as last_error
FROM profile_changes
WHERE change_type = 'bio' 
  AND success = 0
GROUP BY phone
ORDER BY errors DESC;
```

---

## 🚀 Тестирование

### 1. Ручное тестирование /setbio

```bash
# В Telegram боте:
/setbio
# Выбрать аккаунт (например 1)
1
# Ввести новое био
Test bio for verification 🚀
```

**Проверить логи:**
```bash
tail -f bot_logs.txt | grep "PROFILE UPDATE"
```

**Ожидаемые логи:**
```
PROFILE UPDATE: cmd=/setbio, phone=+79123456789, ...
PROFILE UPDATE: Creating client for phone=+79123456789
PROFILE UPDATE: Connecting client...
PROFILE UPDATE: Checking authorization...
PROFILE UPDATE: Getting current profile...
PROFILE UPDATE: Current bio: 'Old bio text'
PROFILE UPDATE: Calling UpdateProfileRequest...
PROFILE UPDATE: Verifying bio change...
PROFILE UPDATE: SUCCESS - Bio VERIFIED changed
PROFILE UPDATE: Old bio: 'Old bio text'
PROFILE UPDATE: New bio: 'Test bio for verification 🚀'
```

### 2. Запуск диагностики всех аккаунтов

```bash
python3 diagnose_setbio.py
```

**Проверить отчёт:**
```bash
ls -lh bio_test_report_*.md
cat bio_test_report_*.md
```

### 3. Проверка rate limiting

```bash
# Быстро 2 раза подряд
/setbio
1
First bio

# Сразу ещё раз
/setbio
1
Second bio

# Должно показать:
# ⏰ Слишком частые операции! Подождите: 59 минут
```

---

## 📋 Чеклист перед использованием

- [ ] `diagnose_setbio.py` запущен и отчёт сгенерирован
- [ ] Проверены все аккаунты на работоспособность
- [ ] Сломанные аккаунты помечены как BROKEN
- [ ] Аккаунты без сессий переавторизованы через `/auth`
- [ ] Rate limiting работает (проверено двойным вызовом)
- [ ] Логи пишутся в `bot_logs.txt`
- [ ] База данных `profile_changes` существует и работает

---

## 🎯 Итог: /setbio теперь

✅ **Максимально надёжная**
- Проверяет реальный результат, не доверяет API слепо
- Детальные ошибки для каждого случая
- Честные ответы пользователю

✅ **Полностью диагностируемая**
- Скрипт тестирования всех аккаунтов
- Детальные логи каждой операции
- MD-отчёты с рекомендациями

✅ **Защищена от лимитов**
- Rate limiting 1 час между изменениями
- Проверка FROZEN аккаунтов
- Обработка FloodWait от Telegram

✅ **Правильная техническая реализация**
- Отдельный клиент для каждого аккаунта
- UpdateProfileRequest(about=...) напрямую
- Верификация через GetFullUserRequest
