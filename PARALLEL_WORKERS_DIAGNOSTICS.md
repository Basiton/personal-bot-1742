# Диагностика параллельной работы аккаунтов

## Проблема
Автокомментинг работает, но комментарии идут только от одного аккаунта, даже если в системе несколько активных аккаунтов и настроен `MAX_ACTIVE_ACCOUNTS > 1` или `/setparallel > 1`.

## Анализ кода

### Как должна работать параллельность

Код в `pro_auto_comment()` (строки 5133+) уже **ПРАВИЛЬНО реализован** для параллельной работы:

1. **Фильтрация активных аккаунтов:**
```python
active_accounts = {phone: data for phone, data in self.accounts_data.items()
                 if data.get('status') == ACCOUNT_STATUS_ACTIVE and data.get('session')}
```

2. **Ограничение по MAX_PARALLEL_ACCOUNTS:**
```python
MAX_PARALLEL_ACCOUNTS = self.max_parallel_accounts
accounts_list = list(active_accounts.items())
num_accounts = min(len(accounts_list), MAX_PARALLEL_ACCOUNTS)
accounts_list = accounts_list[:MAX_PARALLEL_ACCOUNTS]
```

3. **Создание отдельных воркеров:**
```python
for i, (phone, data) in enumerate(accounts_list):
    task = asyncio.create_task(self.account_worker(phone, data, channel_subset))
    tasks.append(task)
```

4. **Параллельный запуск:**
```python
await asyncio.gather(*all_tasks, return_exceptions=True)
```

### Почему может работать только 1 аккаунт

Есть только **ДВЕ** причины, почему может работать только 1 аккаунт:

#### 1. В системе только 1 аккаунт со статусом `active`
Даже если у вас 5 аккаунтов, но только 1 имеет статус `active`, будет создан только 1 воркер.

**Проверка:**
```
/listaccounts
```

Смотрите на колонку "Статус":
- ✅ active - будет работать
- 🔵 reserve - НЕ будет работать
- 🔴 broken - НЕ будет работать

**Решение:**
```
/toggleaccount
→ Выберите аккаунт с reserve
→ Переключится в active
```

#### 2. Установлен `MAX_PARALLEL_ACCOUNTS = 1`
Даже если у вас 5 активных аккаунтов, но `max_parallel_accounts = 1`, будет создан только 1 воркер.

**Проверка:**
Посмотрите в логах при `/startmon`:
```
⚡ Параллельно работают: `1` аккаунтов
```

**Решение:**
```
/setparallel 3  (или любое другое число)
```

## Добавленные улучшения логирования

### 1. При старте `pro_auto_comment()`

```
================================================================================
⚙️  PARALLEL PROCESSING CONFIGURATION
================================================================================
📊 Total active accounts available: 2
⚡ MAX_PARALLEL_ACCOUNTS setting: 2
🎯 Will create workers for: 2 accounts
================================================================================
```

**Если только 1 активный аккаунт:**
```
⚠️⚠️⚠️ ONLY 1 ACTIVE ACCOUNT! ⚠️⚠️⚠️
⚠️ This means NO PARALLEL PROCESSING!
⚠️ To enable parallel work:
⚠️   1. Use /listaccounts to see all accounts
⚠️   2. Use /toggleaccount to activate more accounts
⚠️   3. Or add new accounts with /auth
```

**Если MAX_PARALLEL_ACCOUNTS = 1:**
```
⚠️⚠️⚠️ MAX_PARALLEL_ACCOUNTS = 1 ⚠️⚠️⚠️
⚠️ Even though you have multiple active accounts,
⚠️ only 1 will work due to parallel limit!
⚠️ Use /setparallel <number> to increase (e.g., /setparallel 3)
```

### 2. При создании воркеров

```
================================================================================
🚀 CREATING 2 PARALLEL WORKERS
================================================================================
🔧 Creating worker #1/2 for [Account1]
   Phone: +1234567890
   Status: active
   Channels: 1-50 (50 total)
🔧 Creating worker #2/2 for [Account2]
   Phone: +0987654321
   Status: active
   Channels: 51-100 (50 total)
================================================================================
✅ ALL 2 WORKERS CREATED AND LAUNCHED
================================================================================
```

### 3. При старте каждого воркера

```
============================================================
👷 WORKER STARTED: Account1 (4567890)
📊 Account status: active
👤 Admin ID: 123456789
📢 Assigned channels: 50
   1. @channel1
   2. @channel2
   ... (and 48 more)
============================================================
```

### 4. При отправке комментария

**В тестовом режиме:**
```
🧪 TEST MODE SUCCESS:
   Channel: @testchannel
   Account: Account1 (+1234567890)
   Comment: Отличный пост! 🔥...
   Time: 14:23:45
   Post ID: 12345
```

**В обычном режиме:**
```
✅💬 COMMENT SENT from [Account1] to @channel1
   📱 Phone: +1234567890
   💬 Comment: Интересная статья, спасибо!...
   🆔 Post ID: 12345
   ⏰ Time: 14:23:45
```

## Как проверить параллельность

### Шаг 1: Проверить количество активных аккаунтов
```
/listaccounts
```

Убедитесь, что минимум 2 аккаунта имеют статус ✅ `active`.

Если нет - используйте `/toggleaccount` для активации.

### Шаг 2: Проверить лимит параллельности
```
/setparallel 2  # или больше
```

### Шаг 3: Запустить мониторинг
```
/startmon
```

Посмотрите на вывод:
```
⚡ Параллельно работают: `2` аккаунтов  ← должно быть > 1
```

### Шаг 4: Проверить логи

Ищите в логах:

**При старте:**
```bash
grep "CREATING.*PARALLEL WORKERS" bot.log
```

Должно быть: `🚀 CREATING 2 PARALLEL WORKERS` (или больше)

**Старт воркеров:**
```bash
grep "WORKER STARTED" bot.log
```

Должно быть несколько строк для разных аккаунтов.

**Комментарии от разных аккаунтов:**
```bash
grep "COMMENT SENT from" bot.log
```

Вы должны видеть комментарии от разных аккаунтов:
```
✅💬 COMMENT SENT from [Account1] to @channel1
✅💬 COMMENT SENT from [Account2] to @channel2
✅💬 COMMENT SENT from [Account1] to @channel3
✅💬 COMMENT SENT from [Account2] to @channel4
```

## Типичные ситуации

### ✅ Правильная конфигурация (2 параллельных аккаунта)

**Аккаунты:**
- +1111111111 - ✅ active
- +2222222222 - ✅ active
- +3333333333 - 🔵 reserve
- +4444444444 - 🔵 reserve

**Настройки:**
- `MAX_PARALLEL_ACCOUNTS = 2`

**Результат:**
```
🚀 CREATING 2 PARALLEL WORKERS
👷 WORKER STARTED: +1111111111
👷 WORKER STARTED: +2222222222
```

Комментарии идут от обоих аккаунтов параллельно.

### ❌ Неправильная конфигурация (только 1 активный)

**Аккаунты:**
- +1111111111 - ✅ active
- +2222222222 - 🔵 reserve
- +3333333333 - 🔵 reserve
- +4444444444 - 🔵 reserve

**Настройки:**
- `MAX_PARALLEL_ACCOUNTS = 2`

**Результат:**
```
⚠️⚠️⚠️ ONLY 1 ACTIVE ACCOUNT! ⚠️⚠️⚠️
🚀 CREATING 1 PARALLEL WORKERS
👷 WORKER STARTED: +1111111111
```

Комментарии идут только от одного аккаунта.

**Решение:**
```
/toggleaccount → выбрать +2222222222 → переключится в active
```

### ❌ Неправильная конфигурация (лимит = 1)

**Аккаунты:**
- +1111111111 - ✅ active
- +2222222222 - ✅ active
- +3333333333 - ✅ active

**Настройки:**
- `MAX_PARALLEL_ACCOUNTS = 1`

**Результат:**
```
⚠️⚠️⚠️ MAX_PARALLEL_ACCOUNTS = 1 ⚠️⚠️⚠️
🚀 CREATING 1 PARALLEL WORKERS
👷 WORKER STARTED: +1111111111
```

Комментарии идут только от одного аккаунта.

**Решение:**
```
/setparallel 3
/stopmon
/startmon
```

## Ротация и параллельность

Ротация работает **ВМЕСТЕ** с параллельностью:

1. Если у вас 2 активных + 2 резервных + `MAX_PARALLEL_ACCOUNTS = 2`:
   - Работают аккаунты #1 и #2
   - Через N часов ротация меняет их на #3 и #4
   - Снова работают 2 аккаунта параллельно

2. При ротации:
   - Старые воркеры останавливаются (статус меняется на reserve)
   - Новые воркеры запускаются при следующем цикле

## Статус исправлений

✅ **Код параллельности работает ПРАВИЛЬНО**
✅ **Добавлено детальное логирование**
✅ **Добавлены предупреждения при неправильной конфигурации**
✅ **Логи показывают, от какого аккаунта идет каждый комментарий**

## Что делать, если параллельность не работает

1. **Проверьте статусы аккаунтов:** `/listaccounts` - должно быть несколько ✅ active

2. **Проверьте лимит:** `/setparallel` должен быть > 1

3. **Перезапустите мониторинг:**
   ```
   /stopmon
   /startmon
   ```

4. **Проверьте логи:**
   ```bash
   grep "PARALLEL PROCESSING CONFIGURATION" bot.log -A 10
   grep "WORKER STARTED" bot.log
   grep "COMMENT SENT from" bot.log
   ```

5. **Если видите предупреждение - следуйте инструкциям в нем!**

Дата: 2026-01-17
