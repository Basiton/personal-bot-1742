# Исправление команд управления профилем

## Проблема

При использовании команд `/setbio`, `/setavatar` и `/setname` при выборе 2-го (или любого другого) аккаунта через reply-меню:
- Бот сообщал об успешном обновлении
- Но био, аватар или имя **фактически не обновлялись** в Telegram
- С первым аккаунтом всё работало нормально

## Причины проблемы

### 1. Отсутствие фильтрации по admin_id
```python
# БЫЛО (НЕПРАВИЛЬНО):
all_accounts = [(phone, data) for phone, data in self.accounts_data.items() 
                if data.get('session')]

# СТАЛО (ПРАВИЛЬНО):
admin_id = self.get_admin_id(event.sender_id)
if admin_id is None:  # Super admin - show all
    filtered_accounts = self.accounts_data
else:  # Regular admin - show only their accounts
    filtered_accounts = {phone: data for phone, data in self.accounts_data.items()
                       if data.get('admin_id') == admin_id}
```

**Проблема:** Команды показывали ВСЕ аккаунты из базы, а не только те, что принадлежат текущему админу. Это приводило к несовпадению нумерации с `/listaccounts`.

### 2. Отсутствие сортировки
```python
# БЫЛО (НЕПРАВИЛЬНО):
all_accounts = [(phone, data) for phone, data in self.accounts_data.items()]

# СТАЛО (ПРАВИЛЬНО):
all_accounts = [(phone, data) for phone, data in sorted(filtered_accounts.items())]
```

**Проблема:** Порядок элементов в Python dict не гарантирован. При разных вызовах команды аккаунты могли идти в разном порядке, что приводило к неправильному выбору.

### 3. Недостаточное логирование
Добавлено детальное логирование:
- При выборе аккаунта: индекс, телефон, статус, admin_id, наличие сессии
- При подключении клиента: факт подключения, авторизация
- При вызове API: какой метод вызывается, с какими параметрами
- При успехе/ошибке: детальная информация

## Исправленные команды

### `/setname` - Изменение имени профиля
✅ Теперь фильтрует аккаунты по admin_id  
✅ Использует стабильную сортировку  
✅ Детальное логирование всех операций  

### `/setbio` - Изменение описания (био)
✅ Теперь фильтрует аккаунты по admin_id  
✅ Использует стабильную сортировку  
✅ Детальное логирование всех операций  

### `/setavatar` - Загрузка аватарки
✅ Теперь фильтрует аккаунты по admin_id  
✅ Использует стабильную сортировку  
✅ Детальное логирование всех операций  

## Как работают исправления

### 1. Показ списка аккаунтов
```
/setbio  →  Бот показывает:
1. ✅ +79123456789
2. 🔵 +79987654321
3. ✅ +79111222333
```
- Список формируется так же, как в `/listaccounts`
- Учитывается admin_id текущего пользователя
- Порядок стабильный (sorted)

### 2. Выбор аккаунта
```
User: 2 (reply на сообщение бота)
```
- Бот извлекает аккаунт по индексу: `accounts[2-1]` = `accounts[1]`
- Логирует: `PROFILE UPDATE: Account selected - index=2, phone=+79987654321, status=reserve, admin_id=123456789`

### 3. Ввод данных
```
User: Новое био
```
- Бот создаёт Telethon клиент для выбранного аккаунта
- Логирует: `PROFILE UPDATE: Starting bio update - phone=+79987654321`
- Логирует: `PROFILE UPDATE: Client connected for +79987654321`

### 4. Обновление профиля
```python
await client(UpdateProfileRequest(about=new_bio))
```
- Логирует: `PROFILE UPDATE: Calling UpdateProfileRequest for +79987654321 with bio length=50`
- Логирует: `PROFILE UPDATE: SUCCESS - Bio updated for +79987654321`

## Пример логов

### Успешное обновление
```
INFO: PROFILE UPDATE: Account selected - index=2, phone=+79987654321, status=reserve, admin_id=123456789, has_session=True
INFO: PROFILE UPDATE: Starting bio update - phone=+79987654321, status=reserve, admin_id=123456789
INFO: PROFILE UPDATE: Client connected for +79987654321
INFO: PROFILE UPDATE: Calling UpdateProfileRequest for +79987654321 with bio length=45
INFO: PROFILE UPDATE: SUCCESS - Bio updated for +79987654321
```

### Ошибка
```
ERROR: PROFILE UPDATE: FAILED - Account +79987654321 not authorized
```
или
```
ERROR: PROFILE UPDATE: ERROR - Failed to update bio for +79987654321: Session revoked
ERROR: PROFILE UPDATE: ERROR details - Type: AuthKeyError, Message: Session was revoked
```

## Тестирование

После применения исправлений протестируйте:

1. **Запустите бота:**
   ```bash
   python main.py
   ```

2. **Проверьте список аккаунтов:**
   ```
   /listaccounts
   ```
   Запомните порядок аккаунтов.

3. **Тестируйте /setbio:**
   ```
   /setbio
   Бот: выберите номер аккаунта
   Вы: 2
   Бот: отправьте новое био
   Вы: Тестовое био для 2-го аккаунта
   ```

4. **Проверьте логи:**
   ```bash
   tail -f bot.log | grep "PROFILE UPDATE"
   ```

5. **Проверьте в Telegram:**
   - Откройте профиль 2-го аккаунта
   - Убедитесь, что био обновилось

6. **Тестируйте /setavatar и /setname аналогично**

## Совместимость

Исправления не затрагивают:
- Команды массового обновления (`/setbioall`, `/setavatarall`)
- Хранение данных в `bot_data.json`
- Авторизацию аккаунтов
- Ротацию аккаунтов
- Систему admin_id

Все изменения касаются только:
- Формирования списка аккаунтов для выбора
- Логирования операций с профилем

## Дата исправления
2026-01-17
