# 🎯 Оптимизация комментариев YandexGPT (Женский род + Живость)

## ✅ Что было исправлено

### 1. **Женский род в промпте**
**Проблема:** Комментарии генерировались в мужском роде ("видел", "думал").

**Решение:**
- Добавлено явное указание в промпте: "Пиши в ЖЕНСКОМ РОДЕ (видела, читала, думала)"
- Добавлен запрет: "❌ МУЖСКОЙ РОД (видел, читал, думал - НЕ используй!)"
- Обновлены все типы реакций с указанием женского рода

### 2. **Увеличена эмоциональность**
**Проблема:** Комментарии получались сухими.

**Решение:**
- Повышен параметр `temperature`: **0.8 → 0.85** (больше креативности)
- Увеличен `max_tokens`: **120 → 130** (больше места для эмоций)
- В промпте добавлено: "Добавь чуть эмоций и личного отношения"
- Добавлены примеры эмоциональных фраз: "мне понравилось", "интересно", "не ожидала"

### 3. **Обновлены типы реакций**
Добавлены более эмоциональные и женские типы:
- **Восхищение**: "впечатлилась", "мне понравилось"
- **Резонанс**: "прямо про меня", "тоже думала об этом"
- **Опыт**: "пробовала", "замечала", "сталкивалась"

### 4. **Больше эмодзи**
- Частота эмодзи увеличена: **22% → 38%** (примерно каждый 3-й комментарий)
- Эмодзи делают комментарии более живыми и дружелюбными

### 5. **Fallback комментарии в женском роде**
Обновлены резервные комментарии:
- ❌ "Отличный пост", "Круто написано"
- ✅ "Мне понравилось", "Рада, что прочитала"

---

## 🎨 Дополнительные рекомендации для оптимизации

### 1. **Настройка температуры (temperature)**
Параметр креативности модели (0.0 - 1.0):

```python
# Текущее значение
temperature = 0.85  # Хороший баланс

# Если комментарии слишком шаблонные:
temperature = 0.9-0.95  # Больше разнообразия, но может быть странность

# Если комментарии слишком "безумные":
temperature = 0.7-0.8  # Более предсказуемо
```

**Где изменить:** [main.py](main.py#L103) в функции `generate_neuro_comment()`

### 2. **Длина комментария (max_tokens)**

```python
# Текущее значение
max_tokens = 130  # Короткие, живые комментарии

# Для более развёрнутых комментариев:
max_tokens = 150-180  # Больше деталей

# Для очень кратких:
max_tokens = 100-120  # Быстрые реакции
```

### 3. **Частота эмодзи**

```python
# Текущее значение
use_emoji = random.random() < 0.38  # ~38% (каждый 3-й)

# Для более эмоциональных комментариев:
use_emoji = random.random() < 0.5  # ~50% (каждый 2-й)

# Для более сдержанных:
use_emoji = random.random() < 0.2  # ~20% (каждый 5-й)
```

**Где изменить:** [main.py](main.py#L155) в функции `generate_neuro_comment()`

### 4. **Улучшение промпта**

#### Добавить примеры желаемого стиля:
```python
prompt = f"""...

ПРИМЕРЫ ХОРОШИХ КОММЕНТАРИЕВ:
• "Не ожидала, что Python настолько ускорился!"
• "Прямо про меня, тоже столкнулась с этим"
• "40% — впечатляющий результат 🔥"
• "Пробовала этот рецепт, получилось вкусно"
• "Мне понравился подход, возьму на заметку"

...
"""
```

#### Усилить запрет на формальности:
```python
ЗАПРЕЩЕНО:
❌ МУЖСКОЙ РОД
❌ Формальный тон ("благодарю", "желаю успехов")
❌ Безличные конструкции ("можно заметить", "стоит отметить")
❌ Канцелярит ("в данной статье", "следует подчеркнуть")
```

### 5. **Добавить больше типов реакций**

```python
reaction_types = [
    # Существующие...
    ("сомнение", "мягко усомниться или задать вопрос, используя женский род (не уверена, интересно, а как)"),
    ("совет", "дать короткий дружеский совет из опыта, используя женский род (советую попробовать, мне помогло)"),
    ("ностальгия", "вспомнить похожий опыт из прошлого, используя женский род (вспомнила, как сама)"),
]
```

### 6. **Персонализация под тему канала**

Можно добавить специфичные фразы для каждой темы:

```python
theme_phrases = {
    "программирование": ["пробовала", "тестировала", "дебажила"],
    "кулинария": ["готовила", "пробовала", "экспериментировала"],
    "бизнес": ["применяла", "внедряла", "тестировала"],
    "саморазвитие": ["практикую", "пробовала", "заметила эффект"]
}
```

---

## 🧪 Тестирование

### Запуск теста
```bash
python test_female_comments.py
```

### Что проверяет тест:
- ✅ Комментарии в женском роде
- ✅ Эмоциональность и живость
- ✅ Разнообразие стилей
- ✅ Использование конкретики из постов
- ✅ Частота эмодзи
- ✅ Средняя длина

### Включение подробного логирования:
```bash
export LOG_COMMENTS=true
python test_female_comments.py
```

Это покажет:
- Сырой ответ от YandexGPT
- Финальный комментарий после обработки
- Можно найти, где модель "портит" женский род

---

## 📊 Мониторинг в продакшене

### Проверка качества комментариев в логах:

```bash
# Смотреть комментарии в реальном времени
sudo journalctl -u comapc-bot -f | grep "Финальный комментарий"

# Собрать последние 50 комментариев для анализа
sudo journalctl -u comapc-bot --since "1 hour ago" | grep "Финальный комментарий" | tail -50

# Найти комментарии с мужским родом (если ещё остались)
sudo journalctl -u comapc-bot --since "1 hour ago" | grep "Финальный комментарий" | grep -E "видел|думал|понял|читал"
```

---

## 🔧 Быстрая настройка

### Вариант 1: Максимальная эмоциональность
```python
temperature = 0.9
max_tokens = 150
use_emoji = random.random() < 0.5  # 50%
```

### Вариант 2: Сдержанный стиль
```python
temperature = 0.75
max_tokens = 100
use_emoji = random.random() < 0.2  # 20%
```

### Вариант 3: Сбалансированный (рекомендуется)
```python
temperature = 0.85  # Текущий
max_tokens = 130    # Текущий
use_emoji = random.random() < 0.38  # Текущий
```

---

## ⚠️ Важные замечания

### 1. **Постобработка в humanize_comment()**
Функция `humanize_comment()` может иногда "портить" хорошие комментарии, убирая нужные слова. Если видите проблемы, проверьте правила в этой функции.

### 2. **Баланс API**
YandexGPT - платный сервис. Следите за балансом в Yandex Cloud Console.

### 3. **Rate Limits**
При большом количестве комментариев можете столкнуться с лимитами. Yandex обычно даёт:
- 10 запросов в секунду
- 100,000 токенов в день (для базового плана)

### 4. **Fallback на шаблоны**
При ошибках API бот автоматически переключается на шаблонные комментарии. Они тоже обновлены в женском роде.

---

## 📝 Чеклист после изменений

- [ ] Запустить `python test_female_comments.py`
- [ ] Проверить, что нет мужского рода
- [ ] Оценить эмоциональность (читается ли как живой человек?)
- [ ] Проверить разнообразие (генерировать 10+ комментариев на одну тему)
- [ ] Перезапустить бота: `sudo systemctl restart comapc-bot`
- [ ] Смотреть логи в течение часа: `sudo journalctl -u comapc-bot -f`
- [ ] Проверить реальные комментарии в Telegram

---

## 🎯 Ожидаемые результаты

### До оптимизации:
```
❌ "Интересная статья, много нового узнал"
❌ "Видел похожую информацию"
❌ "Круто"
```

### После оптимизации:
```
✅ "Мне понравился подход, не думала об этом раньше"
✅ "40% ускорение — впечатляет! Надо попробовать 🔥"
✅ "Сама сталкивалась с этой проблемой, спасибо за решение"
✅ "Прямо про меня, тоже заметила этот эффект"
```

---

## 🆘 Проблемы и решения

### Проблема: Всё равно много мужского рода
**Решение:**
1. Увеличьте акцент в промпте (добавьте больше примеров)
2. Добавьте негативные примеры в промпт ("НЕ пиши так: видел, думал, понял")
3. Проверьте, что используется обновлённая версия функции

### Проблема: Комментарии слишком длинные/короткие
**Решение:**
1. Настройте `max_tokens` (100-180)
2. Добавьте в промпт точную длину: "Напиши 7-12 слов"

### Проблема: Слишком много/мало эмодзи
**Решение:**
1. Настройте `use_emoji = random.random() < X` (0.2-0.5)
2. Измените в промпте: "БЕЗ ЭМОДЗИ" или "Используй 1-2 эмодзи"

### Проблема: Комментарии всё равно шаблонные
**Решение:**
1. Повысьте `temperature` до 0.9
2. Добавьте больше типов реакций
3. Добавьте примеры живых комментариев в промпт
4. Проверьте, что YandexGPT работает (не fallback)

---

## 📚 Дополнительные ресурсы

- [Документация YandexGPT](https://cloud.yandex.ru/docs/foundation-models/concepts/yandexgpt/models)
- [Промпт-инжиниринг](https://platform.openai.com/docs/guides/prompt-engineering) (принципы применимы и к YandexGPT)
- Текущий промпт: [main.py](main.py#L158-L184)
- Функция постобработки: [main.py](main.py#L302)

---

**Дата обновления:** 28 января 2026
**Версия:** 2.0 (Женский род + Эмоциональность)
