# Краткая инструкция по тестированию исправлений

## Что исправлено

✅ Команды `/setname`, `/setbio`, `/setavatar` теперь:
- Фильтруют аккаунты по admin_id (как в `/listaccounts`)
- Используют стабильную сортировку (порядок всегда одинаковый)
- Детально логируют все операции

✅ Команда `/setbioall`:
- Использует новый статус `ACCOUNT_STATUS_ACTIVE` вместо старого `active`
- Фильтрует аккаунты по admin_id

## Быстрое тестирование

### 1. Запустите бота
```bash
cd /workspaces/personal-bot-1742
python main.py
```

### 2. Откройте логи в отдельном окне
```bash
tail -f bot.log | grep "PROFILE UPDATE"
```

### 3. Проверьте список аккаунтов
В Telegram боте:
```
/listaccounts
```
Запомните порядок и номера аккаунтов (1, 2, 3, ...)

### 4. Тест изменения био
```
/setbio
```
- Бот покажет список аккаунтов (должен быть такой же, как в `/listaccounts`)
- Ответьте (reply): `2`
- Бот попросит новое био
- Отправьте: `Тестовое био для 2-го аккаунта`

**В логах вы увидите:**
```
PROFILE UPDATE: Account selected - index=2, phone=+799..., status=..., admin_id=...
PROFILE UPDATE: Starting bio update - phone=+799...
PROFILE UPDATE: Client connected for +799...
PROFILE UPDATE: Calling UpdateProfileRequest for +799... with bio length=...
PROFILE UPDATE: SUCCESS - Bio updated for +799...
```

**Проверка:** Откройте профиль 2-го аккаунта в Telegram - био должно измениться!

### 5. Тест загрузки аватарки
```
/setavatar
```
- Выберите номер аккаунта (например, `3`)
- Отправьте фото
- Проверьте, что аватарка загрузилась именно на 3-й аккаунт

### 6. Тест изменения имени
```
/setname
```
- Выберите номер аккаунта (например, `1`)
- Отправьте новое имя: `Иван Петров`
- Проверьте, что имя изменилось на 1-м аккаунте

### 7. Тест массового обновления био
```
/addbio Трейдер
/addbio Инвестор
/setbioall
```
- Проверьте, что био обновилось на всех АКТИВНЫХ аккаунтах
- Проверьте, что RESERVE и BROKEN аккаунты не изменились

## Что проверять в логах

### Успех
```
✅ PROFILE UPDATE: SUCCESS - ...
```

### Проблемы
```
❌ PROFILE UPDATE: FAILED - Account not authorized
❌ PROFILE UPDATE: ERROR - ...
```

## Если что-то не работает

1. **Проверьте логи:**
   ```bash
   grep "PROFILE UPDATE" bot.log
   ```

2. **Проверьте список аккаунтов:**
   ```
   /listaccounts
   ```
   Убедитесь, что у аккаунта есть сессия (авторизован)

3. **Проверьте статус аккаунта:**
   - ✅ ACTIVE - должен работать
   - 🔵 RESERVE - должен работать
   - 🔴 BROKEN - возможны проблемы

4. **Проверьте admin_id:**
   Если вы не супер-админ, вы видите только свои аккаунты

## Ожидаемый результат

После исправлений:
- ✅ Порядок аккаунтов в `/setbio`, `/setavatar`, `/setname` совпадает с `/listaccounts`
- ✅ При выборе номера аккаунта изменения применяются к правильному аккаунту
- ✅ Работает с любым номером аккаунта (1, 2, 3, 4, ...)
- ✅ Работает как с ACTIVE, так и с RESERVE аккаунтами
- ✅ Детальные логи помогают отследить проблемы
