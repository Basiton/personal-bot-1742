# 🎯 Живые комментарии — обновление от 17.01.2026

## ✅ Что сделано

### 1. Исправлен `diagnose_yandex_api.py`

**Проблема:** Скрипт использовал зашитый API-ключ, не проверял переменные окружения.

**Решение:**
- ✅ Приоритет переменных окружения: `YC_API_KEY` → `YANDEX_API_KEY`
- ✅ Приоритет folder: `YC_FOLDER_ID` → `YANDEX_FOLDER_ID`
- ✅ Явная проверка: если ключей нет — понятное сообщение и выход
- ✅ Короткая подсказка при 401: только суть, без длинной портянки

**Использование:**
```bash
# На боевом сервере (если ключи в окружении)
python diagnose_yandex_api.py

# Локально через .env
echo "YC_API_KEY=ваш_ключ" >> .env
echo "YC_FOLDER_ID=ваш_folder" >> .env
python diagnose_yandex_api.py
```

---

### 2. Полностью переработан промпт для комментариев

**Цель:** Убрать «машинный» открыточный стиль, сделать комментарии живыми.

#### Что изменилось в промпте:

**❌ УБРАНО:**
- Формальные фразы: "Желаю успехов", "Здорово, что вы...", "Благодарю"
- Корпоративный тон
- Одинаковые эмодзи 🚀🔥✨
- Длинные инструкции (было 40+ строк)

**✅ ДОБАВЛЕНО:**
- **Рандомизация стиля:** вопрос / реакция на цифры / замечание по сути
- **Запрет шаблонов:** явный список запрещённых фраз
- **Ограничение эмодзи:** максимум 1-2, разные каждый раз
- **Короткий промпт:** сфокусирован на разговорном стиле

**Новая структура промпта:**
```
СТИЛЬ: разговорный, как живой человек — НЕ формальный, НЕ открыточный.

ОБЯЗАТЕЛЬНО:
• 1-2 предложения (20-40 слов)
• Отталкивайся от КОНКРЕТИКИ поста
• [рандомный стиль: вопрос / реакция / замечание]

ЗАПРЕЩЕНО:
❌ "Желаю успехов"
❌ "Здорово, что вы..."
❌ "Рада за вас"
[...и другие формальности]

ЭМОДЗИ: максимум 1-2, разные
```

---

### 3. Добавлена функция `humanize_comment()`

**Постобработка** для «очеловечивания» ответов YandexGPT:

- Убирает формальные конструкции из ответа модели
- Делает текст менее «отредактированным»
- Удаляет лишние запятые (иногда)
- Чистит случайно пробившиеся запрещённые фразы

```python
def humanize_comment(text: str) -> str:
    """
    Постобработка комментария для большей естественности.
    """
    # Убираем лишние пробелы
    text = " ".join(text.split())
    
    # Иногда убираем запятые перед союзами
    if random.random() < 0.3:
        text = text.replace(", а ", " а ").replace(", но ", " но ")
    
    # Удаляем формальные фразы
    formal_replacements = {
        "Желаю вам успехов": "",
        "Желаю успехов": "",
        "Благодарю за пост": "",
        ...
    }
    
    return text.strip()
```

---

### 4. Логирование комментариев (для отладки)

**Новая опция:** `LOG_COMMENTS=true`

Включает детальное логирование сырых ответов YandexGPT и финальных комментариев:

```bash
export LOG_COMMENTS=true
python main.py
```

В логах появится:
```
[INFO] [COMMENT_GEN] Raw: Здорово, что вы планируете 3 проекта...
[INFO] [COMMENT_GEN] Final: 3 проекта + курс до 15 февраля 2026 — амбициозно! Какой первым закроете?
```

Это позволяет:
- Видеть, что модель генерирует изначально
- Понимать, как работает постобработка
- Корректировать промпт на основе реальных примеров

---

### 5. Поддержка `YC_API_KEY` и `YC_FOLDER_ID`

**В `main.py`:**
```python
# Приоритет YC_API_KEY, fallback на YANDEX_API_KEY
YANDEX_API_KEY = os.getenv('YC_API_KEY') or os.getenv('YANDEX_API_KEY', '')
YANDEX_FOLDER_ID = os.getenv('YC_FOLDER_ID') or os.getenv('YANDEX_FOLDER_ID', 'b1g...')
```

Теперь можно использовать:
- Новые переменные: `YC_API_KEY`, `YC_FOLDER_ID` (рекомендуется)
- Старые переменные: `YANDEX_API_KEY`, `YANDEX_FOLDER_ID` (совместимость)

---

## 📊 Примеры: Было → Стало

### Пример 1: Пост про цели до 15 февраля 2026

**Пост:**
> Мои цели до 15 февраля 2026: закончить 3 проекта, запустить курс по маркетингу, запустить сервис аналитики для YouTube.

**❌ Было (старый промпт):**
> Здорово, что вы так основательно подходите к планированию! 👍 Желаю успехов в реализации ваших планов в 2025 году! 🚀

**Проблемы:**
- Год перепутан (2025 ❌)
- Нет конкретики
- Шаблонная фраза "Желаю успехов"

**✅ Стало (новый промпт):**
> 3 проекта + курс + сервис до 15 февраля 2026 — амбициозно! 🚀 Какой из них планируете закрыть первым?

**Улучшения:**
- ✅ Год правильный (2026)
- ✅ Упомянуты все цели
- ✅ Есть вопрос (живой стиль)
- ✅ Нет запрещённых фраз

---

### Пример 2: Пост с цифрами

**Пост:**
> Результаты запуска: 127 регистраций, конверсия 18%, средний чек 2,400 руб.

**❌ Было:**
> Интересные результаты! Спасибо за пост! 👍

**✅ Стало:**
> 18% конверсия при 127 регистрациях — неплохо для первой недели! А онбординг сейчас упрощаете?

---

### Пример 3: Технический пост

**Пост:**
> Перешли с webpack на Vite. Dev server 0.8 сек вместо 12. Потратили 2 дня.

**❌ Было:**
> Классная оптимизация! Желаю успехов! 🔥

**✅ Стало:**
> С 12 секунд до 0.8 — вот это ускорение! 2 дня миграции того стоили?

---

## 🧪 Тестирование

### 1. Диагностика API
```bash
python diagnose_yandex_api.py
```

**Что проверяет:**
- ✅ Наличие `YC_API_KEY` / `YANDEX_API_KEY`
- ✅ Наличие `YC_FOLDER_ID` / `YANDEX_FOLDER_ID`
- ✅ Доступность YandexGPT API
- ✅ Корректность ключей

**Если ключей нет:**
```
❌ ОШИБКА: Переменные окружения не заданы

Необходимо установить:
  - YC_API_KEY (или YANDEX_API_KEY)
  - YC_FOLDER_ID (или YANDEX_FOLDER_ID)
```

---

### 2. Тест комментариев
```bash
python test_improved_comments.py
```

**Что проверяет:**
- ✅ Разговорный стиль (не открыточный)
- ✅ Конкретика из поста
- ✅ Нет запрещённых фраз
- ✅ Длина 20-40 слов
- ✅ Есть вопрос или реакция на данные

**Тестовые сценарии:**
1. Пост с датами (проверка на правильность года)
2. Пост с цифрами (проверка упоминания метрик)
3. Личный опыт (проверка на живой стиль)
4. Технический пост (проверка конкретики)
5. Пост с выбором (проверка ответа на вопрос)

---

## 🚀 Запуск на боевом сервере

### 1. Установите переменные окружения

**Вариант A: Через .env файл**
```bash
nano .env
```

Добавьте:
```env
YC_API_KEY=ваш_актуальный_ключ
YC_FOLDER_ID=ваш_folder_id

# Опционально: логирование комментариев
LOG_COMMENTS=true
```

**Вариант B: Через export**
```bash
export YC_API_KEY="ваш_ключ"
export YC_FOLDER_ID="ваш_folder"
export LOG_COMMENTS="true"  # опционально
```

---

### 2. Проверьте настройку
```bash
python diagnose_yandex_api.py
```

Должен показать:
```
✅ API KEY: AQVN...
✅ FOLDER ID: b1g...
```

---

### 3. Протестируйте комментарии
```bash
python test_improved_comments.py
```

Проверьте, что комментарии:
- ✅ Опираются на конкретику
- ✅ Звучат живо, разговорно
- ✅ Не содержат запрещённых фраз
- ✅ Имеют вопрос или конкретную реакцию

---

### 4. Запустите бота
```bash
python main.py
```

**С логированием комментариев:**
```bash
LOG_COMMENTS=true python main.py
```

Смотрите логи:
```bash
tail -f bot.log | grep "COMMENT_GEN"
```

---

## 📂 Изменённые файлы

| Файл | Что изменено |
|------|-------------|
| [main.py](main.py#L62-L68) | Поддержка `YC_API_KEY`, `YC_FOLDER_ID`, опция `LOG_COMMENTS` |
| [main.py](main.py#L71-L178) | Новая функция `generate_neuro_comment()` с живым промптом |
| [main.py](main.py#L180-L211) | Новая функция `humanize_comment()` для постобработки |
| [diagnose_yandex_api.py](diagnose_yandex_api.py) | Проверка переменных окружения, короткие подсказки |
| [test_improved_comments.py](test_improved_comments.py) | Проверка `YC_API_KEY`, тесты на формальность |

---

## 🎯 Ключевые улучшения

### Стиль комментариев
- ❌ Убраны формальные фразы
- ✅ Разговорный тон
- ✅ Рандомизация (вопрос / реакция / замечание)
- ✅ Ограничение эмодзи (1-2 макс)

### Техническая часть
- ✅ Поддержка `YC_API_KEY` / `YC_FOLDER_ID`
- ✅ Проверка переменных окружения
- ✅ Постобработка `humanize_comment()`
- ✅ Опция логирования `LOG_COMMENTS`

### Диагностика
- ✅ Явные проверки ключей
- ✅ Понятные сообщения об ошибках
- ✅ Без длинных портянок текста

---

## 💡 Для дальнейшей настройки

### Регулировка промпта

Если нужно изменить стиль комментариев, редактируйте [main.py](main.py#L95-L121):

```python
# Добавить новый стиль
style_variants = [
    "с вопросом в конце по теме поста",
    "с реакцией на конкретные цифры, сроки или факты из поста",
    "с коротким замечанием по сути, без вопроса",
    "с личным мини-опытом по теме",  # ← новый вариант
]
```

### Добавление запрещённых фраз

В функции `humanize_comment()` ([main.py](main.py#L193-L201)):

```python
formal_replacements = {
    "Желаю вам успехов": "",
    "Новая фраза для удаления": "",  # ← добавить здесь
    ...
}
```

### Изменение параметров генерации

```python
comment = generate_neuro_comment(
    post_text=post_text,
    channel_theme=channel_theme,
    temperature=0.8,  # ← креативность (0.5-1.0)
    max_tokens=120,   # ← длина (80-150)
)
```

---

## ✅ Чек-лист готовности

- [ ] `YC_API_KEY` установлен в окружении
- [ ] `YC_FOLDER_ID` установлен в окружении
- [ ] `diagnose_yandex_api.py` проходит без ошибок
- [ ] `test_improved_comments.py` генерирует живые комментарии
- [ ] В тестах нет запрещённых фраз
- [ ] Комментарии опираются на конкретику постов
- [ ] Включено логирование (`LOG_COMMENTS=true`) для мониторинга

---

**Дата:** 17 января 2026  
**Статус:** ✅ Готово к использованию  
**Следующий шаг:** Обновить `YC_API_KEY` и протестировать на боевом сервере
