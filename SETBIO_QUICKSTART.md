# 🚀 Быстрый старт: /setbio

## 1️⃣ Первый запуск - диагностика

```bash
cd /workspaces/personal-bot-1742
python3 diagnose_setbio.py
```

**Результат:**
- Файл `bio_test_report_YYYYMMDD_HHMMSS.md` с детальным отчётом
- Список работающих и сломанных аккаунтов
- Конкретные рекомендации для каждого аккаунта

## 2️⃣ Использование /setbio

### В Telegram боте:

```
/setbio
→ Выбрать номер аккаунта (например: 1)
1
→ Ввести новое био
Investor | Trader | Crypto 🚀
```

**Ответ бота:**
```
✅ Био обновлено для +79123456789

Было: Old bio
Стало: Investor | Trader | Crypto 🚀

✅ Изменение подтверждено в Telegram
```

## 3️⃣ Что делать при ошибках

### ❌ "Био слишком длинное"
**Решение:** Сократите до 70 символов

### ⏰ "Флуд-контроль: подождите X минут"
**Решение:** Подождите указанное время ИЛИ используйте другой аккаунт

### 🚫 "Аккаунт заморожен"
**Решение:** Используйте другой аккаунт из списка работающих

### 🔑 "Сессия недействительна"
**Решение:** 
```
/auth +79123456789
```

### ⛔ "Номер забанен"
**Решение:** Пометить аккаунт как broken:
```
/toggleaccount +79123456789
```

## 4️⃣ Проверка логов

```bash
# Смотреть логи в реальном времени
tail -f bot_logs.txt | grep "PROFILE UPDATE"

# Последние 50 записей
tail -50 bot_logs.txt | grep "setbio"
```

## 5️⃣ Лимиты

| Операция | Интервал | Что происходит при нарушении |
|----------|----------|------------------------------|
| /setbio | 1 час | Отказ с точным временем ожидания |
| /setname | 1 час | Отказ с точным временем ожидания |
| /setavatar | 24 часа | Отказ с точным временем ожидания |

**Telegram FloodWait:** Дополнительно может ограничить на 15-60 минут

## 6️⃣ Полезные команды

```bash
# Диагностика
python3 diagnose_setbio.py

# Синтаксис main.py
python3 -m py_compile main.py

# Просмотр отчётов
ls -lh bio_test_report_*.md
cat bio_test_report_*.md

# Логи
tail -f bot_logs.txt

# База данных
sqlite3 bot_advanced.db "SELECT * FROM profile_changes WHERE change_type='bio' ORDER BY change_date DESC LIMIT 10;"
```

## 7️⃣ Типичные сценарии

### Сценарий 1: Первое использование
1. Запустить диагностику: `python3 diagnose_setbio.py`
2. Открыть отчёт и найти работающие аккаунты
3. Использовать `/setbio` с работающим аккаунтом

### Сценарий 2: Флуд-контроль
1. Получили "Подождите X минут"
2. Выбрать другой аккаунт через `/setbio`
3. Или подождать указанное время

### Сценарий 3: Массовое обновление
```
/setbioall
Новое био для всех
```
⚠️ Внимание: Rate limiting работает для каждого аккаунта отдельно

### Сценарий 4: Проверка что изменилось
```
/profile
```
Покажет текущее био всех активных аккаунтов

## 8️⃣ Отчёт о проблемах

**Если что-то не работает:**

1. Проверить логи:
   ```bash
   tail -100 bot_logs.txt | grep -A 5 "ERROR"
   ```

2. Запустить диагностику:
   ```bash
   python3 diagnose_setbio.py
   ```

3. Открыть отчёт и найти конкретную ошибку

4. Следовать рекомендациям из отчёта

## 9️⃣ FAQ

**Q: Почему /setbio говорит успех, но био не меняется?**
A: Теперь НЕ говорит! Система проверяет реальный результат и честно сообщает если Telegram не применил изменение.

**Q: Как часто можно менять био?**
A: 1 раз в час на аккаунт. Telegram может дополнительно ограничить FloodWait.

**Q: Какой максимальный размер био?**
A: 70 символов (ограничение Telegram).

**Q: Можно ли использовать emoji?**
A: Да! Emoji считаются как 1-2 символа.

**Q: Что делать если все аккаунты заморожены?**
A: Подождать 24 часа или добавить новые аккаунты через `/auth`.

**Q: Как узнать текущее био аккаунта?**
A: Команда `/profile` показывает био всех активных аккаунтов.

## 🎯 Готово!

/setbio теперь максимально надёжна и честна. Используйте диагностику перед началом работы и следуйте рекомендациям из отчёта.
