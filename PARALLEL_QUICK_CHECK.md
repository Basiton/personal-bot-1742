# БЫСТРАЯ ПРОВЕРКА: Почему работает только 1 аккаунт?

## Шаг 1: Проверьте активные аккаунты
```
/listaccounts
```

**Смотрите на колонку "Статус":**
- ✅ `active` - аккаунт РАБОТАЕТ
- 🔵 `reserve` - аккаунт НЕ работает (ждет своей очереди)
- 🔴 `broken` - аккаунт сломан

**Сколько у вас аккаунтов со статусом ✅ `active`?**

### Если только 1 аккаунт `active`:
```
/toggleaccount
→ Выберите аккаунт с 🔵 reserve
→ Он переключится в ✅ active
```

Повторите для всех аккаунтов, которые хотите активировать.

## Шаг 2: Проверьте лимит параллельности
```
/setparallel 3
```
(или любое другое число, сколько хотите аккаунтов одновременно)

## Шаг 3: Перезапустите мониторинг
```
/stopmon
/startmon
```

**В ответе посмотрите на строку:**
```
⚡ Параллельно работают: `X` аккаунтов
```

Если `X = 1` - смотрите предупреждения выше этой строки.

## Шаг 4: Проверьте логи

Запустите бота с выводом логов:
```bash
python main.py 2>&1 | tee bot.log
```

После `/startmon` ищите в логах:

### Конфигурация параллельности:
```
⚙️  PARALLEL PROCESSING CONFIGURATION
📊 Total active accounts available: X
⚡ MAX_PARALLEL_ACCOUNTS setting: Y
🎯 Will create workers for: Z accounts
```

**Если `X = 1`** - у вас только 1 активный аккаунт → активируйте больше через `/toggleaccount`

**Если `Y = 1`** - лимит слишком низкий → увеличьте через `/setparallel`

### Создание воркеров:
```
🚀 CREATING N PARALLEL WORKERS
🔧 Creating worker #1/N for [Account1]
🔧 Creating worker #2/N for [Account2]
...
✅ ALL N WORKERS CREATED AND LAUNCHED
```

**Если видите только 1 воркер** - проблема в конфигурации (см. выше).

### Старт воркеров:
```
👷 WORKER STARTED: Account1 (...)
📊 Account status: active
👤 Admin ID: ...

👷 WORKER STARTED: Account2 (...)
📊 Account status: active
👤 Admin ID: ...
```

**Должно быть несколько таких блоков** для разных аккаунтов.

### Комментарии от разных аккаунтов:
```
✅💬 COMMENT SENT from [Account1] to @channel1
   📱 Phone: +111...

✅💬 COMMENT SENT from [Account2] to @channel2
   📱 Phone: +222...

✅💬 COMMENT SENT from [Account1] to @channel3
   📱 Phone: +111...
```

**Вы должны видеть комментарии от РАЗНЫХ аккаунтов.**

## Типичные ошибки

### ❌ "У меня 5 аккаунтов, но работает только 1"

**Проверьте статусы:**
```
/listaccounts
```

Возможно, 4 аккаунта в статусе `reserve` или `broken`.

**Решение:**
```
/toggleaccount → активировать нужные аккаунты
```

### ❌ "Я активировал 3 аккаунта, но работает только 1"

**Проверьте лимит:**
Посмотрите в логах или в ответе `/startmon` на строку:
```
⚡ Параллельно работают: `1` аккаунтов
```

**Решение:**
```
/setparallel 3
/stopmon
/startmon
```

### ❌ "После ротации снова работает 1 аккаунт"

Ротация **НЕ меняет** количество активных аккаунтов - она меняет **КАКИЕ** аккаунты активны.

**Пример:**
- **До ротации:** Account1 (active), Account2 (active), Account3 (reserve), Account4 (reserve)
- **После ротации:** Account1 (reserve), Account2 (reserve), Account3 (active), Account4 (active)

Количество активных остается = 2.

## Код работает правильно!

Параллельность реализована **КОРРЕКТНО**. Если работает только 1 аккаунт, это всегда означает:
- У вас только 1 аккаунт в статусе `active`
- ИЛИ `MAX_PARALLEL_ACCOUNTS = 1`

Проверьте конфигурацию по шагам выше.

---

Дата: 2026-01-17
