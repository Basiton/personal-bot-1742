# 🔒 РУКОВОДСТВО ПО ЗАЩИТЕ СЕССИЙ

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Сохранность сессий и авторизаций аккаунтов — приоритет №1.**

Потеря сессий означает:
- ❌ Необходимость заново логиниться во все аккаунты
- ❌ Простой бота на время переавторизации
- ❌ Риск блокировки при массовых входах

---

## 📋 ОГЛАВЛЕНИЕ

1. [Что такое сессии и где они хранятся](#что-такое-сессии)
2. [Автоматическая защита](#автоматическая-защита)
3. [Команды для работы с бэкапами](#команды-для-работы-с-бэкапами)
4. [Что может привести к потере сессий](#что-может-привести-к-потере-сессий)
5. [Безопасная работа с тестовыми скриптами](#безопасная-работа-с-тестовыми-скриптами)
6. [Восстановление после сбоя](#восстановление-после-сбоя)
7. [Чек-лист безопасности](#чек-лист-безопасности)

---

## 🔑 Что такое сессии

### Где хранятся сессии аккаунтов

Бот использует **StringSession** (текстовые сессии), которые хранятся в:

```
bot_data.json
```

Структура файла:
```json
{
  "accounts": {
    "+79123456789": {
      "session": "1AgAOMTQ5LjE1NC4xNjcuNTEBu...",  ← ЭТО СЕССИЯ!
      "active": true,
      "name": "John",
      "phone": "+79123456789",
      "status": "active"
    }
  },
  "channels": [...],
  "templates": [...]
}
```

**ВНИМАНИЕ:** 
- ❌ `.session` файлы в корне проекта (`+13434919340.session`) — это старый формат, НЕ используется ботом
- ✅ Боту нужен только `bot_data.json`
- ✅ Файл `bot_session.session` — сессия самого бота (не аккаунтов)

---

## 🛡️ Автоматическая защита

### Что делает бот автоматически

#### 1. **Резервное копирование при каждом сохранении**

При каждом вызове `save_data()` бот:

1. Создаёт timestamped бэкап: `bot_data.json.backup_20260119_143025`
2. Обновляет быстрый бэкап: `bot_data.json.bak`
3. Хранит последние 5 автоматических бэкапов
4. Удаляет старые бэкапы автоматически

```python
# Это происходит автоматически!
bot_data.json  →  bot_data.json.backup_20260119_143025
bot_data.json  →  bot_data.json.bak
```

#### 2. **Атомарная запись (Atomic Write)**

Вместо прямой перезаписи `bot_data.json`, бот:

1. Сначала пишет во временный файл: `bot_data.json.tmp`
2. Проверяет успешность записи
3. Только потом заменяет основной файл

**Защита:** Если запись упадёт с ошибкой, `bot_data.json` останется невредим.

#### 3. **Автоматическое восстановление**

Если при загрузке `bot_data.json` оказался повреждён (JSON syntax error), бот:

1. Ищет последний валидный бэкап
2. Автоматически восстанавливает данные
3. Логирует восстановление

---

## 💾 Команды для работы с бэкапами

### `/backup` — Создать бэкап вручную

Создаёт ручную резервную копию прямо сейчас.

```
/backup
```

**Результат:**
```
✅ Резервная копия создана

📁 Файл: bot_data.json.manual_backup_20260119_143530
👥 Аккаунтов сохранено: 4
📊 Каналов: 12
💬 Шаблонов: 10
```

**Когда использовать:**
- ✅ Перед массовыми изменениями (добавление/удаление аккаунтов)
- ✅ Перед экспериментами с профилями
- ✅ Перед обновлением кода
- ✅ Раз в день для надёжности

---

### `/listbackups` — Список всех бэкапов

Показывает все доступные резервные копии.

```
/listbackups
```

**Результат:**
```
💾 РЕЗЕРВНЫЕ КОПИИ

📌 Последний бэкап (.bak):
   bot_data.json.bak
   📅 2026-01-19 14:35:30
   💾 45.3 KB

🖐️ Ручные бэкапы (3):
• manual_backup_20260119_143530
  📅 2026-01-19 14:35:30
  💾 45.3 KB
• manual_backup_20260119_120000
  📅 2026-01-19 12:00:00
  💾 44.8 KB

🤖 Автоматические бэкапы (5):
• backup_20260119_143525
  📅 2026-01-19 14:35:25
  💾 45.3 KB
...
```

---

### `/restore` — Восстановить из бэкапа

Восстанавливает данные из последнего валидного бэкапа.

```
/restore
```

**Результат:**
```
✅ Данные восстановлены

📁 Из файла: bot_data.json.bak
👥 Аккаунтов: 2 → 4
📊 Каналов: 12
💬 Шаблонов: 10

💾 Резервная копия до восстановления:
   bot_data.json.before_restore_20260119_143600

⚠️ ВАЖНО: Перезапустите бота для применения изменений:
   /stopmon затем /startmon
```

**⚠️ ВАЖНО:** После восстановления обязательно перезапустите мониторинг!

---

## ⚠️ Что может привести к потере сессий

### ❌ ОПАСНЫЕ ДЕЙСТВИЯ

#### 1. **Удаление или повреждение `bot_data.json`**

```bash
# ОПАСНО! НЕ ДЕЛАТЬ!
rm bot_data.json
```

**Защита:**
- ✅ Автоматические бэкапы каждые несколько минут
- ✅ Используйте `/backup` перед экспериментами
- ✅ Проверьте `/listbackups` — есть ли свежие бэкапы

#### 2. **Ручное редактирование `bot_data.json` с ошибками**

```json
{
  "accounts": {
    "+79123456789": {
      "session": "incomplete...   ← ОШИБКА: неполная сессия!
```

**Защита:**
- ⚠️ НЕ редактируйте `bot_data.json` вручную
- ✅ Если нужно — создайте `/backup` сначала
- ✅ Проверяйте JSON syntax (используйте редактор с валидацией)

#### 3. **Запуск тестовых скриптов без понимания**

Некоторые скрипты могут создавать НОВЫЕ сессии (пустые):

```python
# ОПАСНО если запустить на боевом окружении
client = TelegramClient(StringSession(''), API_ID, API_HASH)
```

**Безопасные скрипты** (используют существующие сессии):
- ✅ `test_profile_verification.py` — читает из `bot_data.json`
- ✅ `diagnose_setbio.py` — использует существующие сессии
- ✅ `demo_profiles.py` — демо режим, не трогает реальные данные
- ✅ `test_all_accounts_profile.py` — использует сессии из `bot_data.json`

**Потенциально опасные действия:**
- ⚠️ `clean_channels.py` — удаляет каналы (НЕ сессии, но меняет данные)
- ⚠️ Любые скрипты с `os.remove()` — могут удалять файлы

---

### ✅ БЕЗОПАСНЫЕ ДЕЙСТВИЯ

Эти команды и действия НЕ приводят к потере сессий:

#### Команды бота:
- ✅ `/listaccounts` — просто читает данные
- ✅ `/toggleaccount` — меняет статус (не сессию)
- ✅ `/setname`, `/setbio`, `/setavatar` — меняют профиль (не сессию)
- ✅ `/addchannel`, `/delchannel` — работают с каналами (не сессиями)
- ✅ `/stats`, `/accountstats` — только чтение
- ✅ `/startmon`, `/stopmon` — управление мониторингом

#### Операции с профилями:
- ✅ Изменение имени через `/setname`
- ✅ Изменение био через `/setbio`
- ✅ Загрузка аватара через `/setavatar`
- ✅ Все операции через `/accounts`

**Эти действия используют СУЩЕСТВУЮЩИЕ сессии, не создают новые!**

---

## 🧪 Безопасная работа с тестовыми скриптами

### Правило №1: Тестовый режим

Используйте `/testmode` для экспериментов:

```bash
# 1. Включите тестовый режим с одним безопасным каналом
/testmode on @testchannel

# 2. Экспериментируйте сколько нужно

# 3. Выключите тестовый режим
/testmode off
```

**Преимущества:**
- ✅ Все остальные каналы игнорируются
- ✅ Можно ставить низкую скорость
- ✅ Безопасно для тестирования нового функционала

---

### Правило №2: Создавайте бэкап перед тестами

```bash
# ПЕРЕД любым тестом
/backup

# Теперь можно тестировать
python test_profile_verification.py

# Если что-то пошло не так
/restore
```

---

### Правило №3: Проверяйте код скрипта

Перед запуском любого `.py` файла, убедитесь что он:

#### ✅ Использует существующие сессии:

```python
# БЕЗОПАСНО
with open('bot_data.json', 'r') as f:
    data = json.load(f)
    session = data['accounts'][phone]['session']
    
client = TelegramClient(StringSession(session), API_ID, API_HASH)
```

#### ❌ НЕ создаёт пустые сессии:

```python
# ОПАСНО для боевого окружения!
client = TelegramClient(StringSession(''), API_ID, API_HASH)
# Это для НОВОЙ авторизации, не для работы с существующими!
```

#### ❌ НЕ перезаписывает `bot_data.json` без бэкапа:

```python
# ОПАСНО!
with open('bot_data.json', 'w') as f:
    json.dump(new_data, f)  # Может затереть сессии!
```

---

## 🚑 Восстановление после сбоя

### Сценарий 1: `bot_data.json` повреждён

**Симптомы:**
```
❌ bot_data.json corrupted: Expecting property name...
```

**Решение:**
```bash
# 1. Бот попытается восстановить автоматически
# Если не получилось:

# 2. Используйте команду
/restore

# 3. Перезапустите мониторинг
/stopmon
/startmon
```

---

### Сценарий 2: Случайно удалили `bot_data.json`

**Решение:**
```bash
# 1. Найдите последний бэкап вручную
ls -lht bot_data.json.backup_* | head -5

# 2. Скопируйте его в bot_data.json
cp bot_data.json.backup_20260119_143530 bot_data.json

# 3. Перезапустите бота
python3 main.py
```

---

### Сценарий 3: Пришлось переавторизоваться

Если по какой-то причине потеряли все сессии:

```bash
# 1. Создайте бэкап пустого состояния (на всякий случай)
/backup

# 2. Авторизуйте аккаунты заново
/auth +79123456789 socks5:proxy:1080:user:pass

# 3. После каждой авторизации делайте бэкап
/backup

# 4. Проверьте что все аккаунты на месте
/listaccounts
```

---

## ✅ Чек-лист безопасности

### Ежедневно:
- [ ] `/backup` — хотя бы раз в день
- [ ] `/listbackups` — проверьте что бэкапы создаются

### Перед изменениями:
- [ ] `/backup` — перед добавлением/удалением аккаунтов
- [ ] `/backup` — перед экспериментами с профилями
- [ ] `/backup` — перед запуском новых скриптов

### После изменений:
- [ ] `/listaccounts` — проверьте что все аккаунты на месте
- [ ] Проверьте `bot_data.json` — убедитесь что файл не пустой
- [ ] `/backup` — сохраните новое состояние

### Перед тестированием:
- [ ] `/testmode on @testchannel` — изолируйте эксперименты
- [ ] `/backup` — создайте точку восстановления
- [ ] Проверьте код скрипта — что он делает с сессиями

### После проблем:
- [ ] `/restore` — восстановите из бэкапа
- [ ] `/stopmon` и `/startmon` — перезапустите мониторинг
- [ ] `/listaccounts` — убедитесь что все работает

---

## 📊 Структура файлов

### Боевые файлы (НЕ ТРОГАТЬ!):
```
bot_data.json          ← Основной файл с сессиями
bot_session.session    ← Сессия самого бота
```

### Резервные копии (автоматические):
```
bot_data.json.bak                      ← Последний быстрый бэкап
bot_data.json.backup_20260119_143530   ← Timestamped бэкап
bot_data.json.backup_20260119_143525
...
```

### Резервные копии (ручные):
```
bot_data.json.manual_backup_20260119_120000
bot_data.json.manual_backup_20260119_140000
...
```

### Старые .session файлы (не используются):
```
+13434919340.session   ← Старый формат, НЕ нужен боту
+13435909132.session   ← Старый формат, НЕ нужен боту
...
```

**Эти файлы можно удалить, они не используются.**

---

## 🔧 Технические детали

### Как работает автоматический бэкап

При каждом вызове `self.save_data()`:

1. **Создаётся timestamped бэкап:**
   ```python
   timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
   backup_name = f'bot_data.json.backup_{timestamp}'
   shutil.copy2(DB_NAME, backup_name)
   ```

2. **Обновляется .bak файл:**
   ```python
   shutil.copy2(DB_NAME, 'bot_data.json.bak')
   ```

3. **Очищаются старые бэкапы:**
   ```python
   # Хранятся только последние 5
   backup_files = sorted(..., reverse=True)
   for old_backup in backup_files[5:]:
       os.remove(old_backup)
   ```

4. **Атомарная запись:**
   ```python
   # Пишем во временный файл
   with open('bot_data.json.tmp', 'w') as f:
       json.dump(data, f)
   
   # Заменяем основной файл только после успешной записи
   shutil.move('bot_data.json.tmp', 'bot_data.json')
   ```

### Приоритет восстановления

При `self.restore_from_backup()`:

1. Проверяется `bot_data.json.bak` (последний быстрый бэкап)
2. Проверяются последние 3 timestamped бэкапа
3. Берётся первый валидный JSON

---

## 📞 Поддержка

Если возникли проблемы с сессиями:

1. **НЕ ПАНИКУЙТЕ** — бэкапы создаются автоматически
2. Используйте `/listbackups` — посмотрите доступные копии
3. Используйте `/restore` — восстановите данные
4. Если нужно — авторизуйте аккаунты заново через `/auth`

---

## 🎯 Резюме

### ✅ ЧТО ДЕЛАТЬ:
- ✅ Используйте `/backup` регулярно
- ✅ Проверяйте `/listbackups` периодически
- ✅ Используйте `/testmode` для экспериментов
- ✅ Создавайте бэкап перед любыми изменениями
- ✅ Доверяйте автоматическим бэкапам

### ❌ ЧЕГО НЕ ДЕЛАТЬ:
- ❌ НЕ редактируйте `bot_data.json` вручную
- ❌ НЕ удаляйте `bot_data.json` без бэкапа
- ❌ НЕ запускайте непонятные скрипты на боевом окружении
- ❌ НЕ игнорируйте ошибки восстановления

---

**Версия:** 1.0  
**Дата:** 19 января 2026  
**Статус:** Активная защита включена ✅
