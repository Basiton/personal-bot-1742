# Полное сохранение состояния бота между перезапусками

## ✅ Что реализовано

Теперь бот **полностью сохраняет своё состояние** между перезапусками. Все настройки автоматически сохраняются в файл `config.json` и восстанавливаются при следующем запуске.

## 📁 Структура файлов

### 1. `config.json` — Настройки бота
Автоматически создаётся при первом запуске. Хранит все настройки:

```json
{
  "speed": 20,
  "max_parallel_accounts": 2,
  "rotation_interval": 14400,
  "worker_mode": "distributed",
  "max_cycles_per_worker": 3,
  "worker_recovery_enabled": true,
  "test_mode": false,
  "test_channels": [],
  "test_mode_speed_limit": 10,
  "active_accounts": ["+13434919340", "+15482373234"],
  "last_updated": "2026-01-26T12:00:00"
}
```

### 2. `bot_data.json` — Данные аккаунтов и каналов
Хранит:
- Аккаунты с сессиями
- Список каналов
- Шаблоны комментариев
- Админы

### 3. `bot_session.session` — Сессия самого бота
Telethon-сессия бота (SQLite), автоматически создаётся и переиспользуется.

## 🔧 Настройки, которые сохраняются

### Основные настройки

| Параметр | Команда | Описание |
|----------|---------|----------|
| `speed` | `/setratelimit 20` | Скорость комментариев (комм/час на аккаунт) |
| `max_parallel_accounts` | `/setparallel 2` | Количество одновременно работающих аккаунтов |
| `rotation_interval` | `/setrotation 14400` | Интервал ротации аккаунтов (секунды) |

### Режим воркеров

| Параметр | Команда | Описание |
|----------|---------|----------|
| `worker_mode` | `/setworkermode distributed` | Режим распределения каналов |
| `max_cycles_per_worker` | `/setmaxcycles 3` | Количество циклов перед ротацией |
| `worker_recovery_enabled` | `/togglerecovery` | Автовосстановление после бана |

### Тестовый режим

| Параметр | Команда | Описание |
|----------|---------|----------|
| `test_mode` | `/testmode on` | Включен ли тестовый режим |
| `test_channels` | `/testmode on @channel1` | Список тестовых каналов |
| `test_mode_speed_limit` | `/testmode speed 10` | Лимит скорости в тесте |

### Активные аккаунты

| Параметр | Команда | Описание |
|----------|---------|----------|
| `active_accounts` | `/toggleaccount +1234567890` | Список активных аккаунтов (автоматически) |

## 📋 Команды для работы с настройками

### Просмотр конфигурации

```bash
/config
```

Показывает все текущие настройки в удобном формате:
- Основные настройки
- Режим воркеров
- Тестовый режим
- Статистику аккаунтов
- Информацию о файлах

### Изменение настроек

Все команды **автоматически сохраняют** изменения в `config.json`:

**Скорость:**
```bash
/setratelimit 25         # Устанавливает скорость 25 комм/час
```

**Параллельность:**
```bash
/setparallel 3           # 3 аккаунта одновременно
```

**Ротация:**
```bash
/setrotation 21600       # Ротация каждые 6 часов
```

**Режим воркеров:**
```bash
/setworkermode cyclic         # Для тестов (5-20 каналов)
/setworkermode distributed    # Для продакшна (100+ каналов)
```

**Циклы:**
```bash
/setmaxcycles 5          # Каждый воркер работает 5 циклов
/setmaxcycles 0          # Бесконечная работа
```

**Автовосстановление:**
```bash
/togglerecovery          # Вкл/выкл автовосстановление
```

**Тестовый режим:**
```bash
/testmode on @channel1 @channel2    # Включить тест на каналах
/testmode speed 15                   # Скорость в тесте
/testmode off                        # Выключить тест
```

## 🔄 Как это работает

### При запуске бота:

1. **Загружается `config.json`**
   - Если файла нет — создаётся с дефолтными значениями
   - Если файл повреждён — восстанавливается из бэкапа `config.json.bak`
   - Недостающие параметры добавляются автоматически

2. **Применяются настройки**
   - `self.messages_per_hour = config['speed']`
   - `self.max_parallel_accounts = config['max_parallel_accounts']`
   - И так далее для всех параметров

3. **Синхронизируются активные аккаунты**
   - Список активных аккаунтов из `bot_data.json` сохраняется в `config['active_accounts']`

### При изменении настроек:

1. **Команда изменяет значение**
   - Например: `self.messages_per_hour = 25`

2. **Автоматически сохраняется в конфиг**
   - `self.save_config_value('speed', 25)`

3. **Создаётся бэкап**
   - Старая версия сохраняется в `config.json.bak`

4. **Пользователь получает подтверждение**
   - "💾 Настройка сохранена в config.json"

## 🔒 Безопасность сессий

### ✅ Что сохраняется на диске:

1. **Сессия бота** — `bot_session.session`
   - Автоматически создаётся и переиспользуется
   - Не требует повторного логина

2. **Сессии аккаунтов** — в `bot_data.json`
   - Хранятся как `StringSession` (зашифрованная строка)
   - Автоматически восстанавливаются при перезапуске

### ⚠️ Важно:

**НЕ УДАЛЯЙТЕ** следующие файлы без бэкапа:
- `bot_session.session` — авторизация бота
- `bot_data.json` — сессии аккаунтов
- `config.json` — настройки

## 📦 Резервное копирование

### Автоматическое:

1. **При каждом сохранении `config.json`:**
   - Создаётся `config.json.bak`

2. **При каждом сохранении `bot_data.json`:**
   - Создаётся `bot_data.json.autobak`

### Ручное:

```bash
/backup    # Создаёт timestamped бэкап
```

Создаёт файл вида: `bot_data_safe.backup_20260126_120000.json`

**Важно:** Команда `/backup` НЕ сохраняет сессии (только каналы и шаблоны).

## 🚀 Типичный workflow

### Первоначальная настройка:

1. **Запуск бота** (создаётся `config.json` с дефолтами)
2. **Настройка скорости:** `/setratelimit 20`
3. **Настройка параллельности:** `/setparallel 2`
4. **Настройка режима:** `/setworkermode distributed`
5. **Авторизация аккаунтов:** `/auth +1234567890`
6. **Активация аккаунтов:** `/toggleaccount +1234567890`
7. **Добавление каналов:** `/addchannel @channel`
8. **Запуск мониторинга:** `/startmon`

### После `git pull` и перезапуска:

```bash
systemctl restart your-bot
```

**Бот автоматически:**
- ✅ Загружает все настройки из `config.json`
- ✅ Восстанавливает сессии аккаунтов
- ✅ Активирует те же аккаунты, что были активны
- ✅ Применяет тот же режим работы
- ✅ Продолжает работу с той же скоростью

**Никакой ручной настройки не требуется!**

## 🔍 Проверка состояния

### Команда `/config`:

```
⚙️ ТЕКУЩАЯ КОНФИГУРАЦИЯ БОТА:

📊 ОСНОВНЫЕ НАСТРОЙКИ:
  • Скорость: 20 комм/час на аккаунт
  • Параллельность: 2 аккаунтов
  • Ротация: 14400 сек (4ч)

🔄 РЕЖИМ ВОРКЕРОВ:
  • Режим: distributed
  • Макс циклов: 3
  • Автовосстановление: ✅ Вкл

🧪 ТЕСТОВЫЙ РЕЖИМ:
  • Статус: 🔴 Выключен

👥 АККАУНТЫ:
  • Активных: 2 / 2
  • Всего: 5
  • В конфиге: 2

📁 ДАННЫЕ:
  • Каналов: 120
  • Шаблонов: 10
  • Админов: 1

💾 ФАЙЛЫ:
  • config.json: ✅ Есть
  • bot_data.json: ✅ Есть
  • Обновлен: 26.01.2026 12:00

💡 Версия конфига: 1.0
```

## ⚙️ Расширение конфигурации

### Добавление новых параметров:

1. **В `config_manager.py`:**
   ```python
   DEFAULT_CONFIG = {
       ...
       'new_parameter': default_value,
   }
   ```

2. **В `main.py` (`__init__`):**
   ```python
   self.new_parameter = self.config.get('new_parameter', default_value)
   ```

3. **При изменении:**
   ```python
   self.save_config_value('new_parameter', new_value)
   ```

Конфиг автоматически обновится, добавив недостающие поля при следующей загрузке.

## 🐛 Решение проблем

### Конфиг потерялся / повреждён:

1. **Автоматически восстановится из `config.json.bak`**
2. Если и бэкап повреждён — создастся новый с дефолтами

### Сессии слетели:

1. **Проверка:** `/verify_sessions`
2. **Переавторизация:** `/auth +1234567890`

### Настройки не применяются:

1. **Проверьте:** `/config`
2. **Перезапустите мониторинг:** `/stopmon` → `/startmon`
3. **Проверьте права на файлы:** `ls -l config.json bot_data.json`

## 📊 Итоговый чеклист

- [x] ✅ Настройки сохраняются автоматически
- [x] ✅ Сессии Telethon на диске
- [x] ✅ Активные аккаунты в конфиге
- [x] ✅ Тестовый режим сохраняется
- [x] ✅ Режим воркеров сохраняется
- [x] ✅ Скорость/лимиты сохраняются
- [x] ✅ Автоматические бэкапы
- [x] ✅ Команда `/config` для проверки
- [x] ✅ Восстановление после перезапуска
- [x] ✅ Без ручного редактирования файлов

## 🎯 Главное

**Теперь после `git pull` и `systemctl restart` бот поднимается с теми же настройками, что были до перезапуска. Ручное редактирование файлов не требуется!**
