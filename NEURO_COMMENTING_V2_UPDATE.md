# Обновление системы нейрокомментирования v2.0

## Дата: 27 января 2026

## Обзор изменений

Система нейрокомментирования была полностью переработана для работы с несколькими аккаунтами с защитой от самокомментирования, улучшенным качеством комментариев и контролем эмодзи.

---

## 1. Защита от самокомментирования и петель между аккаунтами

### Что было сделано:

#### 1.1 Система учёта своих аккаунтов
- Добавлено сохранение `user_id` для каждого аккаунта при авторизации
- Создан метод `get_my_account_ids()` - возвращает все свои аккаунты с их ID
- Создан метод `is_my_account(user_id, username)` - проверяет, свой ли это аккаунт

#### 1.2 Анализ истории комментариев
- Метод `get_recent_thread_authors(client, discussion_entity, limit=5)`:
  - Получает список последних авторов комментариев в треде
  - Определяет, какие из них - свои аккаунты
  - Возвращает детальную информацию: `user_id`, `username`, `is_mine`, `phone`, `message_id`

#### 1.3 Логика предотвращения петель
Метод `can_account_comment_on_post()` проверяет три условия:

1. **Не комментировать подряд**: Аккаунт не пишет, если он уже оставлял комментарий в последних 3 сообщениях
2. **Не создавать монолог**: Если последние 2+ сообщения только от своих аккаунтов - пауза
3. **Чередование аккаунтов**: Избегать ситуации A → B → A → B (петля между двумя аккаунтами)

### Как это работает:

```python
# В worker перед генерацией комментария:
recent_authors = await self.get_recent_thread_authors(client, discussion_entity, limit=5)
can_comment, reason = self.can_account_comment_on_post(phone, discussion_entity.id, recent_authors)

if not can_comment:
    logger.warning(f"⛔ Пропускаю канал: {reason}")
    continue  # Переходим к следующему каналу
```

---

## 2. Контроль эмодзи

### Что было сделано:

#### 2.1 Ограничение в промпте YandexGPT
- Эмодзи используются только в ~20-25% комментариев (1 из 4-5)
- В промпт добавлено явное указание: "БЕЗ ЭМОДЗИ" или "Максимум 1 эмодзи в КОНЦЕ текста"

#### 2.2 Пост-обработка в `humanize_comment()`
Функция теперь контролирует эмодзи на уровне кода:

1. **Поиск всех эмодзи**: Регулярное выражение находит все эмодзи в тексте
2. **Удаление "стеков"**: Повторяющиеся эмодзи (🔥🔥🔥) нормализуются до одного
3. **Случайное удаление**: С вероятностью 75% убираются даже одиночные эмодзи
4. **Финальная очистка**: Если эмодзи больше 1 - все удаляются, иногда оставляется один в конце

### Результат:
- До: почти каждый комментарий с 1-3 эмодзи
- После: ~20% комментариев с максимум 1 эмодзи в конце

---

## 3. Улучшение качества комментариев

### 3.1 Новая система типов реакций

Добавлено 6 типов реакций для разнообразия:

```python
reaction_types = [
    ("согласие", "выразить согласие с идеей поста"),
    ("уточнение", "задать короткий уточняющий вопрос"),
    ("эмоция", "выразить лёгкую эмоцию (удивление, интерес)"),
    ("благодарность", "поблагодарить за информацию (кратко)"),
    ("скептицизм", "высказать лёгкий скептицизм"),
    ("опыт", "упомянуть свой опыт по теме")
]
```

При каждой генерации случайно выбирается тип реакции для большего разнообразия.

### 3.2 Обновлённый промпт

**Новые требования:**
- Минимум 5-7 слов в комментарии
- Привязка к конкретному содержанию (даты, цифры, факты)
- Разнообразие: избегать шаблонов ("Круто", "Огонь", "Ого")
- Естественность: без агрессии и политики
- Запрет формальных фраз ("Желаю успехов", "Благодарю за пост")

**Структура промпта:**
```
Напиши короткий осмысленный комментарий к Telegram-посту.

СТИЛЬ: простой разговорный, как живой человек пишет в чате.

ТИП КОММЕНТАРИЯ: [выбранный тип реакции]

ТРЕБОВАНИЯ:
✓ Минимум 5-7 слов, максимум 2-3 предложения
✓ Привязан к СОДЕРЖАНИЮ поста
✓ Использовать конкретику: даты, цифры, факты
✓ Разнообразие: избегать шаблонов
...
```

### 3.3 Дедупликация комментариев

Добавлена система предотвращения дубликатов:

**Хранилище:**
```python
self.recent_comments = {}  # {channel_username: [(text, timestamp, phone), ...]}
self.recent_comments_limit = 20  # Последние 20 комментариев на канал
```

**Метод `is_comment_duplicate()`:**
1. Проверяет минимальную длину (5-7 слов)
2. Нормализует текст (удаляет пробелы, эмодзи, приводит к lowercase)
3. Сравнивает с последними 20 комментариями в канале
4. Проверяет точное совпадение и похожесть (>80% общих слов)

**Интеграция в worker:**
```python
for attempt in range(3):  # До 3 попыток
    temp_comment = generate_neuro_comment(...)
    is_dup, reason = self.is_comment_duplicate(username, temp_comment)
    
    if not is_dup:
        comment = temp_comment
        break
    else:
        logger.warning(f"Дубликат: {reason} (попытка {attempt+1}/3)")

if not comment:  # Если не получили уникальный за 3 попытки
    continue  # Пропускаем пост
```

### 3.4 Сохранение истории

После успешной отправки комментария:
```python
self.add_comment_to_history(username, comment, phone)
```

Это гарантирует, что следующие комментарии не будут повторяться.

---

## 4. Технические детали

### 4.1 Новые методы класса `UltimateCommentBot`:

1. `get_my_account_ids()` - список своих аккаунтов
2. `is_my_account(user_id, username)` - проверка своего аккаунта
3. `get_recent_thread_authors(client, entity, limit)` - авторы последних комментариев
4. `can_account_comment_on_post(phone, entity_id, authors)` - проверка петель
5. `is_comment_duplicate(channel, text, min_words)` - проверка дубликатов
6. `add_comment_to_history(channel, text, phone)` - сохранение в историю

### 4.2 Обновлённые функции:

**`generate_neuro_comment()`:**
- Добавлен параметр `comment_type` для выбора типа реакции
- Убраны эмодзи из fallback-комментариев
- Контроль эмодзи: используются в ~22% случаев

**`humanize_comment()`:**
- Добавлен полный контроль эмодзи (regex + удаление стеков)
- Случайное удаление эмодзи (75% вероятность)
- Нормализация повторяющихся эмодзи

**`verify_account_auth()`:**
- Теперь возвращает `user_id` вместе с `name` и `username`
- Обновлено сохранение в `accounts_data`

### 4.3 Новые данные в аккаунтах:

Каждый аккаунт теперь содержит:
```python
{
    'session': '...',
    'name': 'Имя',
    'username': '@username',
    'user_id': 123456789,  # ← НОВОЕ
    'status': 'active',
    'proxy': '...'
}
```

---

## 5. Логирование и отладка

### Новые логи:

```
✅ [Account] Комментарий уникален (попытка 1/3)
⚠️ [Account] Дубликат комментария: exact_duplicate_from_+1234567890 (попытка 2/3)
⛔ [Account] Пропускаю @channel: account_already_commented_recently
⛔ [Account] Пропускаю @channel: too_many_own_accounts_in_row
⛔ [Account] Пропускаю @channel: avoid_own_account_loop
```

---

## 6. Тестирование

### Рекомендуемые сценарии:

1. **Проверка петель:**
   - Запустить 2-3 аккаунта
   - Убедиться, что они не создают диалоги друг с другом
   - Проверить логи на сообщения "avoid_own_account_loop"

2. **Проверка эмодзи:**
   - Сгенерировать 20-30 комментариев
   - Подсчитать процент комментариев с эмодзи
   - Должно быть ~20-25%

3. **Проверка качества:**
   - Проверить минимальную длину (5-7 слов)
   - Убедиться, что нет шаблонных фраз
   - Проверить разнообразие типов реакций

4. **Проверка дедупликации:**
   - В одном канале отправить комментарии от разных аккаунтов
   - Убедиться, что они не повторяются
   - Проверить логи на "is_dup" сообщения

---

## 7. Примеры работы

### Пример 1: Защита от самокомментирования

**Ситуация:** Аккаунт A написал комментарий, следующий аккаунт - тоже A

**Результат:**
```
⛔ [Account A] Пропускаю @channel: account_already_commented_recently
```

### Пример 2: Защита от петель

**Ситуация:** Последние 2 комментария от аккаунтов A и B (оба свои)

**Результат:**
```
⛔ [Account C] Пропускаю @channel: too_many_own_accounts_in_row
```

### Пример 3: Дедупликация

**Попытка 1:** "Интересная новость!"
```
⚠️ Дубликат: exact_duplicate_from_+1234567890 (попытка 1/3)
```

**Попытка 2:** "Классная статья!"
```
⚠️ Дубликат: similar_duplicate_85%_from_+9876543210 (попытка 2/3)
```

**Попытка 3:** "Хорошо написано, спасибо за обзор"
```
✅ Комментарий уникален (попытка 3/3)
```

---

## 8. Конфигурация

Все настройки хранятся в `bot_data.json` и автоматически загружаются при запуске:

```json
{
  "accounts": {
    "+1234567890": {
      "name": "Account1",
      "username": "@user1",
      "user_id": 123456789,
      "session": "...",
      "status": "active"
    }
  }
}
```

---

## 9. Совместимость

✅ Все изменения обратно совместимы
✅ Старые аккаунты автоматически получат `user_id` при следующей проверке `/verify_sessions`
✅ Существующие функции работают как раньше

---

## 10. Известные ограничения

1. **Первый запуск:** Для работы защиты от самокомментирования нужно хотя бы раз запустить `/verify_sessions` чтобы получить `user_id` для всех аккаунтов
2. **История комментариев:** Хранится только в памяти (теряется при перезапуске)
3. **Язык:** Дедупликация работает лучше всего на русском языке

---

## Заключение

Обновление v2.0 делает систему нейрокомментирования значительно более естественной и безопасной:

- ✅ Нет петель между своими аккаунтами
- ✅ Контроль эмодзи (~20% вместо ~90%)
- ✅ Качественные осмысленные комментарии
- ✅ Нет дубликатов
- ✅ Минимум 5-7 слов в каждом комментарии
- ✅ 6 типов реакций для разнообразия

Система готова к использованию с несколькими аккаунтами! 🚀
