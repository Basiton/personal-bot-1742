# 🧪 БЕЗОПАСНЫЙ ЗАПУСК ТЕСТОВЫХ СКРИПТОВ

## ⚠️ ВАЖНО: Прочитайте перед запуском любого скрипта!

Все тестовые скрипты в этом проекте **БЕЗОПАСНЫ** для боевых сессий, но требуют правильного использования.

---

## ✅ БЕЗОПАСНЫЕ СКРИПТЫ (используют существующие сессии)

Эти скрипты **НЕ СОЗДАЮТ** новые сессии, а используют данные из `bot_data.json`:

### Диагностика профилей:
```bash
python3 test_profile_verification.py   # Проверка возможности изменения профиля
python3 diagnose_setbio.py             # Диагностика изменения BIO
python3 test_all_accounts_profile.py   # Тест всех аккаунтов
python3 test_profile_detailed.py       # Детальная проверка профиля
```

### Демонстрация:
```bash
python3 demo_profiles.py               # Демо функционала (тестовые данные)
```

### Управление каналами:
```bash
python3 clean_channels.py              # Очистка нерабочих каналов
python3 find_my_channels.py            # Поиск каналов
python3 search_channels.py             # Поиск каналов по теме
```

### Диагностика:
```bash
python3 check_testmode.py              # Проверка тестового режима
python3 diagnose_yandex_api.py         # Проверка Yandex GPT
python3 test_yandex_gpt.py             # Тест генерации комментариев
```

**Все эти скрипты ЧИТАЮТ сессии из `bot_data.json` и НЕ ПЕРЕЗАПИСЫВАЮТ их.**

---

## 🛡️ Перед запуском ЛЮБОГО скрипта

### Шаг 1: Создайте бэкап

```bash
# Через бота (рекомендуется)
/backup

# Или вручную
cp bot_data.json bot_data.json.manual_backup_$(date +%Y%m%d_%H%M%S)
```

### Шаг 2: Проверьте что скрипт делает

Откройте файл и проверьте:

#### ✅ БЕЗОПАСНО - использует существующие сессии:
```python
# Читает из bot_data.json
with open('bot_data.json', 'r') as f:
    data = json.load(f)
    session = data['accounts'][phone]['session']

# Использует существующую сессию
client = TelegramClient(StringSession(session), API_ID, API_HASH)
```

#### ❌ ОПАСНО - создаёт новую сессию:
```python
# Создаёт ПУСТУЮ сессию (для новой авторизации)
client = TelegramClient(StringSession(''), API_ID, API_HASH)
# ↑ Это нормально ТОЛЬКО для /auth в боте!
```

#### ❌ ОПАСНО - перезаписывает bot_data.json:
```python
# Перезаписывает файл
with open('bot_data.json', 'w') as f:
    json.dump(new_data, f)
# ↑ Проверьте что new_data содержит ВСЕ сессии!
```

### Шаг 3: Запускайте в тестовом режиме (если возможно)

```bash
# Включите тестовый режим в боте
/testmode on @testchannel

# Запустите скрипт
python3 test_profile_verification.py

# Выключите тестовый режим
/testmode off
```

---

## 📋 Описание каждого скрипта

### `test_profile_verification.py`
**Назначение:** Проверяет возможность изменения профиля (имя, био, аватар)  
**Безопасность:** ✅ Читает сессии из `bot_data.json`, не создаёт новые  
**Изменяет профиль?** Да, но временно (для проверки работоспособности)  
**Перезаписывает сессии?** Нет  

**Как запускать:**
```bash
python3 test_profile_verification.py
```

---

### `diagnose_setbio.py`
**Назначение:** Диагностирует возможность изменения BIO для всех аккаунтов  
**Безопасность:** ✅ Читает сессии из `bot_data.json`  
**Изменяет профиль?** Да, тестирует изменение BIO  
**Перезаписывает сессии?** Нет  

**Как запускать:**
```bash
python3 diagnose_setbio.py
```

**Вывод:** Создаёт отчёт `bio_test_report_*.md`

---

### `test_all_accounts_profile.py`
**Назначение:** Проверяет все аккаунты на возможность изменения имени  
**Безопасность:** ✅ Читает из `bot_data.json`  
**Изменяет профиль?** Да, тестирует UpdateProfileRequest  
**Перезаписывает сессии?** Нет  

**Как запускать:**
```bash
python3 test_all_accounts_profile.py
```

---

### `clean_channels.py`
**Назначение:** Удаляет нерабочие каналы из списка  
**Безопасность:** ✅ Читает сессии, изменяет только список каналов  
**Изменяет сессии?** Нет  
**Изменяет bot_data.json?** Да - удаляет нерабочие каналы (НЕ сессии!)  

**⚠️ ВНИМАНИЕ:** Скрипт изменяет список каналов в `bot_data.json`

**Как запускать:**
```bash
# 1. Сделайте бэкап
/backup

# 2. Запустите скрипт
python3 clean_channels.py

# 3. Скрипт спросит подтверждение перед удалением
# 4. Введите 'yes' только если уверены
```

---

### `demo_profiles.py`
**Назначение:** Демонстрация функционала профилей  
**Безопасность:** ✅ Использует ТЕСТОВЫЕ данные из env переменных  
**Изменяет реальные сессии?** Нет  
**Безопасно для боевого окружения?** Да  

**Как запускать:**
```bash
python3 demo_profiles.py
```

---

## 🚫 Что НЕ нужно запускать на боевом окружении

### Скрипты для разработки (если они появятся):
- `migrate_*.py` - скрипты миграции
- `reset_*.py` - скрипты сброса данных
- `init_*.py` - скрипты инициализации

**Если сомневаетесь - спросите или сделайте `/backup`!**

---

## 🔍 Как проверить безопасность скрипта самостоятельно

### 1. Откройте файл
```bash
cat test_profile_verification.py | head -50
```

### 2. Ищите ключевые строки

#### ✅ БЕЗОПАСНО:
```python
with open('bot_data.json', 'r') as f:        # ЧИТАЕТ
    data = json.load(f)
    
StringSession(account_data['session'])       # Использует существующую сессию
```

#### ⚠️ ПРОВЕРЬТЕ:
```python
with open('bot_data.json', 'w') as f:        # ПИШЕТ
    json.dump(data, f)
    # ↑ Убедитесь что data содержит ВСЕ сессии!
```

#### ❌ ОПАСНО для боевого:
```python
StringSession('')                             # Создаёт ПУСТУЮ сессию
# ↑ Это для НОВОЙ авторизации!

os.remove('bot_data.json')                   # УДАЛЯЕТ файл
# ↑ НЕ запускайте без понимания!
```

---

## 💡 Правила безопасности

### Золотое правило:
> **Перед запуском любого `.py` файла → `/backup`**

### Правило тестирования:
> **Сначала `/testmode on @testchannel`, потом тест**

### Правило восстановления:
> **Если что-то сломалось → `/restore` немедленно**

---

## 🔧 Если скрипт сломал данные

### Немедленные действия:

```bash
# 1. СТОП - не паникуйте!

# 2. Восстановите через бота
/restore

# 3. Или вручную (если бот не отвечает)
cp bot_data.json.bak bot_data.json

# 4. Перезапустите бота
pkill -9 -f "python.*main.py"
python3 main.py &

# 5. Проверьте аккаунты
/listaccounts
```

---

## 📊 Проверка после запуска скрипта

Всегда проверяйте после запуска:

```bash
# 1. Проверьте что файл не пустой
ls -lh bot_data.json
# Должен быть примерно такой же размер как до запуска

# 2. Проверьте содержимое
head -20 bot_data.json
# Должны видеть структуру: "accounts", "channels", etc.

# 3. Проверьте через бота
/listaccounts
# Все аккаунты должны быть на месте
```

---

## 🎯 Быстрая справка

| Скрипт | Безопасность | Что делает | Нужен бэкап? |
|--------|--------------|------------|--------------|
| `test_profile_verification.py` | ✅ | Проверяет profile operations | Желательно |
| `diagnose_setbio.py` | ✅ | Тестирует BIO | Желательно |
| `test_all_accounts_profile.py` | ✅ | Проверяет все аккаунты | Желательно |
| `clean_channels.py` | ⚠️ | Удаляет каналы | **Обязательно** |
| `demo_profiles.py` | ✅ | Демо режим | Нет |
| `search_channels.py` | ✅ | Поиск каналов | Нет |
| `check_testmode.py` | ✅ | Проверка режима | Нет |

---

## ✅ Чек-лист перед запуском

- [ ] Прочитал описание скрипта выше
- [ ] Понял что скрипт делает
- [ ] Создал `/backup`
- [ ] Включил `/testmode` (если применимо)
- [ ] Готов к `/restore` если что-то пойдёт не так

---

## 🆘 Экстренная помощь

Если скрипт:
- ❌ Удалил `bot_data.json` → `/restore`
- ❌ Повредил JSON → `/restore`
- ❌ Очистил сессии → `/restore`
- ❌ Бот не видит аккаунты → `/restore`

**В 99% случаев `/restore` решает проблему!**

---

**Связанные документы:**
- [SESSION_PROTECTION_GUIDE.md](SESSION_PROTECTION_GUIDE.md) - Полное руководство
- [SESSION_PROTECTION_QUICK.md](SESSION_PROTECTION_QUICK.md) - Быстрая памятка

**Помните:** Автоматические бэкапы создаются при каждом сохранении, но лучше перестраховаться и сделать `/backup` вручную!
