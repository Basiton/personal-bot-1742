# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: /backup и /restore

## ⚠️ ЧТО БЫЛО НЕ ТАК

Предыдущая версия `/backup` и `/restore` была **опасна**:

❌ Бэкапировала **весь** `bot_data.json` включая сессии  
❌ При восстановлении восстанавливала **старые невалидные сессии**  
❌ Это приводило к **разлогу всех аккаунтов**  
❌ После `/restore` приходилось заново логиниться  

**Проблема:** Вы потеряли 70 шаблонов и список каналов из-за восстановления старого бэкапа со старыми сессиями.

---

## ✅ ЧТО ИСПРАВЛЕНО

### Новая механика (БЕЗОПАСНАЯ):

#### `/backup` теперь бэкапирует ТОЛЬКО:
- ✅ `channels` (список каналов)
- ✅ `templates` (шаблоны комментариев)
- ✅ `bio_links` (ссылки для био)
- ✅ `admins` (список админов)

#### `/backup` НЕ бэкапирует:
- 🔒 **`accounts`** (сессии аккаунтов) — они святое!
- 🔒 Никакие авторизационные данные

#### `/restore` восстанавливает ТОЛЬКО:
- ✅ Каналы из бэкапа
- ✅ Шаблоны из бэкапа
- ✅ Bio-ссылки и админов

#### `/restore` НЕ трогает:
- 🔒 **Сессии аккаунтов** — остаются нетронутыми!
- 🔒 Все аккаунты остаются авторизованными

---

## 🎯 ГЛАВНОЕ ОТЛИЧИЕ

### Было (ОПАСНО):
```json
// bot_data.json.backup_старый
{
  "accounts": {
    "+79123456789": {
      "session": "старая_невалидная_сессия_123",  ← ОПАСНО!
      ...
    }
  },
  "channels": [...],
  "templates": [...]
}
```

**При `/restore`:** Восстанавливалась старая невалидная сессия → разлог!

---

### Стало (БЕЗОПАСНО):
```json
// bot_data_safe.backup_новый.json
{
  "channels": [...],      ← Только данные
  "templates": [...],     ← Только данные
  "bio_links": [...],
  "admins": [...]
  // НЕТ "accounts"!  ← Сессии не включены!
}
```

**При `/restore`:** Восстанавливаются только данные, сессии остаются в оригинальном `bot_data.json`!

---

## 🔄 НОВЫЕ ФАЙЛЫ БЭКАПОВ

### Безопасные бэкапы (новые):
```
bot_data_safe.bak.json                    ← Последний быстрый бэкап (без сессий)
bot_data_safe.backup_20260119_153000.json ← Timestamped бэкапы (без сессий)
bot_data_safe.backup_20260119_154500.json
...
```

### Старые опасные бэкапы (УДАЛИТЬ!):
```
bot_data.json.bak                    ← Содержит старые сессии (ОПАСНО!)
bot_data.json.backup_*               ← Содержат старые сессии (ОПАСНО!)
bot_data.json.manual_backup_*        ← Содержат старые сессии (ОПАСНО!)
```

**⚠️ РЕКОМЕНДАЦИЯ:** Удалите старые бэкапы, чтобы случайно не восстановить из них:
```bash
rm bot_data.json.bak
rm bot_data.json.backup_*
rm bot_data.json.manual_backup_*
```

---

## 📋 КАК ИСПОЛЬЗОВАТЬ СЕЙЧАС

### 1. Создание безопасного бэкапа

```
/backup
```

**Результат:**
```
✅ Безопасная резервная копия создана

📁 Файл: bot_data_safe.backup_20260119_153000.json
📊 Каналов: 12
💬 Шаблонов: 70
🔗 Bio-ссылок: 3
👑 Админов: 2

🔒 Сессии аккаунтов НЕ включены (безопасно!)
```

---

### 2. Просмотр бэкапов

```
/listbackups
```

**Результат:**
```
💾 БЕЗОПАСНЫЕ РЕЗЕРВНЫЕ КОПИИ
🔒 Содержат только данные (каналы, шаблоны)
✅ Сессии аккаунтов НЕ включены

📌 Последний бэкап (.bak):
   bot_data_safe.bak.json
   📅 2026-01-19 15:30:00
   💾 12.5 KB

🖐️ Все бэкапы (5):
• 20260119_153000
  📅 2026-01-19 15:30:00
  💾 12.5 KB
...
```

---

### 3. Восстановление из бэкапа

```
/restore
```

**Результат:**
```
✅ Данные восстановлены

📁 Из файла: bot_data_safe.bak.json

📊 Каналов: 5 → 12
💬 Шаблонов: 10 → 70
🔗 Bio-ссылок: 3
👑 Админов: 2

🔒 Сессии аккаунтов НЕ затронуты!
👥 Все аккаунты (4) остались авторизованы

✅ Можно сразу продолжать работу или /startmon
```

**ВАЖНО:** Сессии остались нетронутыми! Не нужно переавторизовываться!

---

## ✅ ПРОВЕРКА ПОСЛЕ /restore

После восстановления проверьте:

```bash
# 1. Аккаунты на месте и авторизованы?
/listaccounts

# 2. Каналы восстановлены?
/listchannels

# 3. Шаблоны восстановлены?
/listtemplates

# 4. Всё работает?
/startmon
```

**Все аккаунты должны быть авторизованы!** ✅

---

## 🔒 ГАРАНТИИ БЕЗОПАСНОСТИ

### ✅ Теперь БЕЗОПАСНО:
- `/backup` **никогда** не трогает сессии
- `/restore` **никогда** не затрагивает `accounts` в `bot_data.json`
- Сессии всегда остаются в оригинальном состоянии
- После `/restore` не нужна переавторизация
- Можно сразу `/startmon`

### 🔒 Что происходит с сессиями:
```
bot_data.json (на диске)
├── accounts: {...сессии...}  ← НИКОГДА не трогается при /restore
├── channels: [...]            ← Восстанавливается из бэкапа
├── templates: [...]           ← Восстанавливается из бэкапа
└── ...
```

---

## 📊 ЧТО БЭКАПИРУЕТСЯ / ЧТО НЕТ

| Данные | Бэкапируется? | Восстанавливается? | Безопасно? |
|--------|---------------|-------------------|------------|
| **Сессии аккаунтов** | ❌ Нет | ❌ Нет | ✅ Безопасно |
| Каналы | ✅ Да | ✅ Да | ✅ Безопасно |
| Шаблоны | ✅ Да | ✅ Да | ✅ Безопасно |
| Bio-ссылки | ✅ Да | ✅ Да | ✅ Безопасно |
| Админы | ✅ Да | ✅ Да | ✅ Безопасно |

---

## 🎯 СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### Сценарий 1: Случайно удалили каналы

```bash
# О нет! Случайно удалил все каналы!
/delchannel @channel1
...

# Восстанавливаем из бэкапа
/restore

# ✅ Каналы вернулись!
# ✅ Аккаунты остались авторизованы!
```

---

### Сценарий 2: Эксперименты с шаблонами

```bash
# Перед экспериментами
/backup

# Экспериментируем...
/addtemplate Новый шаблон
/edittemplate 1 Изменённый
/cleartemplates  # О нет!

# Откатываемся
/restore

# ✅ Старые шаблоны вернулись!
# ✅ Аккаунты в порядке!
```

---

### Сценарий 3: Перед массовыми изменениями

```bash
# Сначала бэкап
/backup

# Потом меняем что угодно
/addchannel @new1
/addchannel @new2
...
/addtemplate Шаблон 1
...

# Если что-то пошло не так
/restore

# ✅ Всё откатилось!
```

---

## 🚨 ВАЖНО: Автоматические бэкапы ОТКЛЮЧЕНЫ

**Изменение:** Автоматические бэкапы при каждом `save_data()` **отключены**.

**Почему:**
- Они создавали полные копии с сессиями
- При восстановлении из них разлогинивало аккаунты
- Опасны для безопасности сессий

**Теперь:**
- Бэкапы создаются **только вручную** через `/backup`
- Каждый бэкап — безопасный (без сессий)
- Полный контроль над процессом

---

## 💡 РЕКОМЕНДАЦИИ

### Когда делать `/backup`:

✅ **Перед массовыми изменениями:**
- Добавление/удаление многих каналов
- Экспорт/импорт данных
- Эксперименты с шаблонами

✅ **Регулярно:**
- Раз в день (если много изменений)
- После успешной настройки
- Перед обновлением кода

❌ **НЕ нужно бэкапить:**
- Перед каждым комментарием (избыточно)
- Перед `/startmon` (не меняет данные)
- Перед просмотром статистики

---

## 🔍 ТЕХНИЧЕСКАЯ ИНФОРМАЦИЯ

### Формат безопасного бэкапа:

```json
{
  "channels": [
    "@channel1",
    "@channel2"
  ],
  "templates": [
    "Отличный пост! 👍",
    "Спасибо за контент!"
  ],
  "bio_links": [
    "t.me/mychannel"
  ],
  "admins": [
    6730216440,
    5912533270
  ]
}
```

**Обратите внимание:** Нет поля `"accounts"`!

---

### Алгоритм `/restore`:

```python
# 1. Загружаем только данные из бэкапа
backup_data = {
    'channels': [...],
    'templates': [...],
    'bio_links': [...],
    'admins': [...]
}

# 2. Восстанавливаем ТОЛЬКО эти поля
self.channels = backup_data['channels']
self.templates = backup_data['templates']
self.bio_links = backup_data['bio_links']
self.admins = backup_data['admins']

# 3. ВАЖНО: self.accounts_data НЕ трогаем!
# Сессии остаются нетронутыми!

# 4. Сохраняем (включая нетронутые accounts)
self.save_data()
```

---

## ✅ ИТОГ

### Проблема решена:
- ✅ `/backup` больше не трогает сессии
- ✅ `/restore` не затрагивает авторизацию
- ✅ Аккаунты всегда остаются залогиненными
- ✅ Можно безопасно восстанавливать данные
- ✅ Никаких разлогов!

### Теперь можно:
- Безопасно делать `/backup` когда угодно
- Восстанавливать `/restore` без страха
- Экспериментировать с каналами/шаблонами
- Не бояться потерять сессии

---

**Статус:** ✅ ИСПРАВЛЕНО И БЕЗОПАСНО  
**Дата:** 19 января 2026  
**Проверено:** Сессии защищены, разлог невозможен
