# 🔒 ЗАЩИТА СЕССИЙ - КРАТКАЯ ПАМЯТКА

## ⚡ Быстрый старт

### Три команды которые нужно знать:

```bash
/backup          # Создать резервную копию СЕЙЧАС
/listbackups     # Посмотреть все бэкапы
/restore         # Восстановить из последнего бэкапа
```

---

## 🛡️ Автоматическая защита ВКЛЮЧЕНА

Бот автоматически:
- ✅ Создаёт бэкап ПЕРЕД каждым сохранением
- ✅ Хранит последние 5 автоматических бэкапов
- ✅ Создаёт `bot_data.json.bak` для быстрого восстановления
- ✅ Использует атомарную запись (не повредит файл при сбое)
- ✅ Восстанавливает автоматически при повреждении

**Вы защищены даже если забудете делать бэкапы вручную!**

---

## 📋 Когда делать `/backup`

### Обязательно:
- 🔴 Перед добавлением/удалением аккаунтов
- 🔴 Перед запуском тестовых скриптов
- 🔴 Перед любыми экспериментами

### Желательно:
- 🟡 Раз в день (для надёжности)
- 🟡 Перед изменением профилей аккаунтов
- 🟡 После успешной настройки нового функционала

### Не обязательно:
- 🟢 Перед `/startmon` или `/stopmon`
- 🟢 Перед просмотром статистики
- 🟢 Перед добавлением/удалением каналов

---

## ❌ Что приведёт к потере сессий

| Действие | Опасность | Защита |
|----------|-----------|--------|
| Удаление `bot_data.json` | 🔴 КРИТИЧНО | Автобэкапы спасут |
| Повреждение JSON | 🔴 КРИТИЧНО | Автовосстановление |
| Запуск непонятного скрипта | 🟡 СРЕДНЕ | Сначала `/backup` |
| Ручное редактирование файла | 🟡 СРЕДНЕ | Не делайте это |
| Команды бота (`/setname` и т.д.) | 🟢 БЕЗОПАСНО | Всё ок |

---

## ✅ Безопасные команды (можно смело использовать)

**Работа с аккаунтами:**
- `/listaccounts` - просмотр
- `/toggleaccount` - смена статуса
- `/setname`, `/setbio`, `/setavatar` - изменение профиля

**Мониторинг:**
- `/startmon`, `/stopmon` - запуск/остановка
- `/stats`, `/accountstats` - статистика

**Каналы:**
- `/addchannel`, `/delchannel`, `/listchannels`

**Все эти команды НЕ СОЗДАЮТ новые сессии, используют существующие!**

---

## 🚑 Что делать если что-то пошло не так

### Вариант 1: Через бота (рекомендуется)

```bash
# 1. Восстановите из бэкапа
/restore

# 2. Перезапустите мониторинг
/stopmon
/startmon

# 3. Проверьте что всё на месте
/listaccounts
```

### Вариант 2: Вручную (если бот не отвечает)

```bash
# 1. Найдите последний бэкап
ls -lht bot_data.json.backup_* | head -1

# 2. Скопируйте его
cp bot_data.json.backup_20260119_143530 bot_data.json

# 3. Перезапустите бота
pkill -9 -f "python.*main.py"
python3 main.py &
```

---

## 📁 Где находятся сессии

```
bot_data.json              ← ОСНОВНОЙ ФАЙЛ (сессии здесь!)
bot_data.json.bak          ← Последний автобэкап
bot_data.json.backup_*     ← Timestamped бэкапы (5 штук)
bot_data.json.manual_*     ← Ваши ручные бэкапы
```

**Старые `.session` файлы в корне НЕ используются ботом!**

---

## 🧪 Тестирование без риска

```bash
# 1. Включите тестовый режим
/testmode on @testchannel

# 2. Сделайте бэкап
/backup

# 3. Тестируйте сколько нужно

# 4. Если что-то сломалось
/restore

# 5. Выключите тестовый режим
/testmode off
```

---

## 🎯 Главное правило

> **ПЕРЕД ЛЮБЫМИ ЭКСПЕРИМЕНТАМИ → `/backup`**

Всего одна команда спасёт от часов переавторизации!

---

## 📞 Быстрая проверка здоровья

```bash
# Проверьте что бэкапы создаются
/listbackups

# Должны видеть свежие файлы (сегодняшние)
# Если последний бэкап старше 1 дня - что-то не так!
```

---

## 💡 Лайфхаки

### Привычка делать бэкапы:
```bash
# Утром:
/backup

# Перед любыми изменениями:
/backup

# Вечером:
/backup
```

### Проверка перед сном:
```bash
/listbackups      # Есть ли свежие бэкапы?
/listaccounts     # Все аккаунты на месте?
```

---

## 🔍 Признаки проблем

Если видите:
- ❌ `Нет авторизованных аккаунтов` (хотя были)
- ❌ `bot_data.json corrupted`
- ❌ Бот не видит аккаунты после перезапуска

**Действуйте немедленно:**
```bash
/restore
```

---

## ✨ Система работает

Вы защищены на нескольких уровнях:

1. **Автоматические бэкапы** - каждое сохранение
2. **Атомарная запись** - не повредит при сбое
3. **Автовосстановление** - при повреждении файла
4. **Ручные бэкапы** - `/backup` когда нужно
5. **Команда восстановления** - `/restore` одной кнопкой

**Просто используйте `/backup` перед экспериментами - и спите спокойно! 😴**

---

**Полная документация:** [SESSION_PROTECTION_GUIDE.md](SESSION_PROTECTION_GUIDE.md)
