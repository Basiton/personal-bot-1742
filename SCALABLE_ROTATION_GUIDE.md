# 🚀 Руководство по Масштабируемой Рассылке с Ротацией

## 📋 Обзор

Система автоматической рассылки/комментирования теперь поддерживает масштабирование с умным управлением аккаунтами, rate limiting и автоматической ротацией.

## 🎯 Ключевые Возможности

### 1️⃣ **Лимиты Скорости**
- **Диапазон:** 20-40 сообщений в час на аккаунт
- **По умолчанию:** 20 msg/час (консервативный режим)
- **Индивидуально:** Каждый активный аккаунт имеет свой независимый лимит
- **Автоматика:** Система автоматически отслеживает отправки за последний час

#### Команды:
```
/setratelimit 20    # Установить лимит (20-40)
/getratelimit       # Посмотреть текущий лимит
/accountstats       # Детальная статистика по аккаунтам
```

### 2️⃣ **Статусы Аккаунтов**

| Статус | Значок | Описание |
|--------|--------|----------|
| `active` | 🟢 | Участвует в рассылке прямо сейчас |
| `reserve` | 🔵 | В резерве, ждет активации |
| `broken` | 🔴 | Сломан/забанен, нуждается в проверке |

#### Команды:
```
/listaccounts       # Все аккаунты с их статусами
/activeaccounts     # Только активные аккаунты
/reserveaccounts    # Только резервные аккаунты
/blockedaccounts    # Сломанные/заблокированные
/toggleaccount +79123456789  # Переключить статус active ⇄ reserve
```

### 3️⃣ **Параметр MAX_ACTIVE_ACCOUNTS**
- **По умолчанию:** 2 аккаунта
- **Гибкость:** Легко изменяется через команду или настройку
- **Масштабируемость:** Логика работает с любым количеством (2, 3, 5, 10+)

#### Команды:
```
/setparallel 2      # Установить количество одновременно активных
/getparallel        # Посмотреть текущие настройки
```

### 4️⃣ **Автоматическая Ротация**
- **Интервал:** По умолчанию 4 часа (14400 секунд)
- **Цикличность:** Автоматическая смена активных аккаунтов на резервные
- **Гибкость:** Работает с любым количеством аккаунтов

#### Как это работает:
1. Каждые N часов система автоматически:
   - Выводит часть активных аккаунтов в резерв
   - Активирует следующие по списку из резерва
   - Поддерживает `MAX_ACTIVE_ACCOUNTS` активными
2. Уведомляет владельца о ротации
3. Работает в фоновом режиме параллельно с комментированием

#### Команды:
```
/setrotation 14400  # Установить интервал (в секундах, мин 1ч)
/getrotation        # Посмотреть текущий интервал и статус
/rotatenow          # Выполнить ротацию немедленно
```

### 5️⃣ **Авто-Подмена Упавших Аккаунтов**

Система автоматически отслеживает критические ошибки:
- `USER_DEACTIVATED` — аккаунт деактивирован
- `AUTH_KEY_UNREGISTERED` — ключ авторизации недействителен
- `Account Ban` — аккаунт забанен

#### Что происходит при падении:
1. Аккаунт помечается как `broken` (🔴)
2. Немедленно активируется первый доступный `reserve` аккаунт
3. Владелец получает уведомление о замене
4. Если нет резервов — критическое уведомление

### 6️⃣ **Защита от Спама Между Своими Аккаунтами**

**Проблема:** Несколько ваших аккаунтов могут комментировать один чат подряд, что выглядит подозрительно.

**Решение:**
- Система отслеживает последний комментарий в каждом чате
- Если другой ваш аккаунт пытается комментировать слишком быстро:
  - Минимальный интервал: **5 минут** между своими аккаунтами
  - Аккаунт пропускает этот чат и переходит к следующему
  - Логируется предупреждение

#### Настройка:
```python
MIN_INTERVAL_BETWEEN_OWN_ACCOUNTS = 300  # 5 минут (секунды)
```

## 🔧 Настройки по Умолчанию

```python
# Константы в main.py
MIN_MESSAGES_PER_HOUR = 20           # Минимум msg/час
MAX_MESSAGES_PER_HOUR = 40           # Максимум msg/час
DEFAULT_MESSAGES_PER_HOUR = 20       # По умолчанию

DEFAULT_MAX_ACTIVE_ACCOUNTS = 2       # Активных аккаунтов одновременно
DEFAULT_ROTATION_INTERVAL = 14400     # Ротация каждые 4 часа (секунды)
MIN_INTERVAL_BETWEEN_OWN_ACCOUNTS = 300  # 5 мин между своими аккаунтами
```

## 📊 Логирование

Система логирует все ключевые события:

### ✅ Успешные операции:
```
✅ Account John activated from reserve
🔄 Account Mary: active → reserve (Scheduled rotation)
🟢 Account Tom → ACTIVE
✅ Rotation completed. Current status: {...}
```

### ⚠️ Ограничения:
```
⏳ [Account] Rate limit reached. Waiting 1234s
🛡️ [Account] Another account commented in channel recently. Waiting 180s
```

### 🔴 Критические события:
```
🔴 Account permanently banned!
⚠️ Replaced broken account: John → Reserve1
🚨 ВНИМАНИЕ: Нет резервных аккаунтов!
```

### 📈 Статистика:
```
📊 Each account handles ~15 channels
⚡ Rate limit: 20 msg/hour per account
🛡️ Anti-spam: 300s between own accounts in same chat
```

## 🚀 Типичный Сценарий Работы

### Начальная Настройка:
```bash
/setparallel 3          # 3 активных аккаунта
/setratelimit 25        # 25 сообщений/час на каждый
/setrotation 21600      # Ротация каждые 6 часов
```

### Запуск:
```bash
/startmon               # Запуск с автоматической ротацией
```

### Что происходит:
1. **Первые 3 аккаунта становятся active** (🟢)
   - Остальные автоматически в reserve (🔵)
2. **Каждый аккаунт работает независимо:**
   - Отправляет до 25 msg/час
   - Проверяет лимит перед каждым комментарием
   - Ждет если лимит достигнут
3. **Защита от спама:**
   - Если Account1 комментировал chat_X
   - Account2 подождет 5 минут перед комментом в chat_X
4. **Через 6 часов автоматическая ротация:**
   - Account1 → reserve
   - Account4 → active
   - Уведомление владельцу
5. **Если Account2 падает:**
   - Account2 → broken
   - Account5 (из reserve) → active
   - Немедленное уведомление

## 📱 Полный Список Новых Команд

### Управление Лимитами:
```
/setratelimit 25       # Установить лимит msg/час (20-40)
/getratelimit          # Текущий лимит
/accountstats          # Статистика активности всех аккаунтов
```

### Управление Ротацией:
```
/setrotation 21600     # Установить интервал ротации (секунды)
/getrotation           # Текущий интервал и статус
/rotatenow             # Выполнить ротацию немедленно
```

### Управление Аккаунтами:
```
/listaccounts          # Все аккаунты (🟢🔵🔴)
/activeaccounts        # Только активные (🟢)
/reserveaccounts       # Только резервные (🔵)
/blockedaccounts       # Сломанные (🔴)
/toggleaccount +7XXX   # Переключить active ⇄ reserve
```

### Настройки Параллелизма:
```
/setparallel 3         # Количество одновременно активных
/getparallel           # Текущие настройки
```

## 🎯 Рекомендации

### Безопасный Режим (Низкий Риск):
```
/setparallel 2         # 2 аккаунта
/setratelimit 20       # 20 msg/час
/setrotation 14400     # 4 часа
```

### Средний Режим (Умеренный Риск):
```
/setparallel 3         # 3 аккаунта
/setratelimit 30       # 30 msg/час
/setrotation 21600     # 6 часов
```

### Агрессивный Режим (Высокий Риск):
```
/setparallel 5         # 5 аккаунтов
/setratelimit 40       # 40 msg/час
/setrotation 28800     # 8 часов
```

## ⚡ Производительность

### Примеры расчета (безопасный режим):
- **2 active аккаунта × 20 msg/час = 40 msg/час** всего
- **40 msg/час × 24 часа = 960 комментариев/день**
- **Ротация каждые 4 часа = 6 ротаций/день**
- **Все аккаунты используются циклично**

### С 6 аккаунтами (2 active + 4 reserve):
- День 1: Account1, Account2 (active)
- Через 4ч: Account3, Account4 (active)
- Через 8ч: Account5, Account6 (active)
- Через 12ч: Account1, Account2 (снова active)

## 🛡️ Безопасность

### Встроенные Механизмы:
1. ✅ **Rate Limiting** — предотвращает флуд
2. ✅ **Anti-Spam Protection** — свои аккаунты не спамят друг друга
3. ✅ **Автоматическая Ротация** — распределяет нагрузку
4. ✅ **Авто-Подмена** — мгновенная замена упавших
5. ✅ **Детальное Логирование** — отслеживание всех событий

## 📝 Миграция Со Старой Версии

При первом запуске новой версии:
1. Старое поле `active: true/false` автоматически конвертируется в `status: 'active'/'reserve'`
2. Первые `MAX_ACTIVE_ACCOUNTS` становятся `active`
3. Остальные автоматически `reserve`
4. Данные сохраняются

**Никаких ручных действий не требуется!**

## 🔍 Troubleshooting

### Проблема: "No active accounts found!"
**Решение:** 
```
/listaccounts          # Проверить статусы
/toggleaccount +7XXX   # Активировать нужные
```

### Проблема: "No reserve accounts available"
**Решение:**
- Добавить больше аккаунтов через `/auth`
- Или уменьшить `/setparallel`

### Проблема: Аккаунты комментируют слишком медленно
**Решение:**
```
/setratelimit 30       # Увеличить лимит (осторожно!)
/accountstats          # Проверить загрузку
```

### Проблема: Аккаунт помечен как broken но работает
**Решение:**
```
/toggleaccount +7XXX   # Вернуть в reserve
/toggleaccount +7XXX   # Активировать
```

## 🎓 Заключение

Новая система обеспечивает:
- ✅ **Масштабируемость** — работает с любым количеством аккаунтов
- ✅ **Безопасность** — множество защитных механизмов
- ✅ **Автоматизацию** — минимум ручного управления
- ✅ **Гибкость** — легко настраивается под ваши нужды
- ✅ **Надежность** — авто-восстановление при сбоях

**Запускайте, настраивайте и наслаждайтесь автоматической рассылкой! 🚀**
