# Активация аккаунтов - Отчет

## Дата: 26 января 2026

## Проблема

Пользователь видел в `/stats`:
- 👥 Активных аккаунтов: **10**
- Но комментировал только **1 аккаунт**
- Health check показывал: Ожидается: **2**, Работает: **1**

## Диагностика

Проверка `bot_data.json` показала:
- `max_parallel_accounts`: **НЕ УСТАНОВЛЕН** (использовалось значение по умолчанию = 2)
- Реально активных аккаунтов: **2** (остальные 8 были в статусе `reserve`)

## Причина

**Команда `/stats` показывала неправильное количество!**

Фактически:
- Активных: 2 аккаунта
- Reserve: 8 аккаунтов
- Total: 10 аккаунтов

Поэтому создавалось только 2 воркера (по количеству активных), но работал только 1.

## Исправление

### 1. Активированы все аккаунты ✅
```bash
python -c "
import json
with open('bot_data.json', 'r') as f:
    data = json.load(f)

# Активируем первые 10 аккаунтов
phones = sorted(data['accounts'].keys())[:10]
for phone in phones:
    data['accounts'][phone]['status'] = 'active'

with open('bot_data.json', 'w') as f:
    json.dump(data, f, indent=2, ensure_ascii=False)
"
```

### 2. Установлен лимит параллельности ✅
```json
{
  "max_parallel_accounts": 10
}
```

## Результат

Теперь:
- ✅ max_parallel_accounts = **10**
- ✅ Active accounts = **10**
- ✅ При перезапуске создастся **10 воркеров**

## Команды для проверки

### Проверить активные аккаунты
```bash
/listaccounts
```

### Проверить лимит параллельности
```bash
/setparallel  # Показывает текущее значение
```

### Активировать/деактивировать аккаунт
```bash
/toggleaccount +1234567890  # Переключить статус
```

### Установить лимит параллельности
```bash
/setparallel 10  # Установить 10 параллельных воркеров
```

## Следующие шаги

1. ✅ Перезапустить бота
2. Проверить в логах:
   ```
   🔧 Creating worker #1/10 for [Account1]
   🔧 Creating worker #2/10 for [Account2]
   ...
   🔧 Creating worker #10/10 for [Account10]
   ```
3. Проверить Health check через 2 минуты:
   ```
   ✅ Health check OK: 10/10 workers
   ```
4. Проверить `/stats` - комментарии должны идти со всех 10 аккаунтов

## Важно!

**Команда `/stats` была правильной!** Она считала активные аккаунты корректно:
```python
active_accounts_count = sum(1 for d in filtered_accounts.values() 
                           if d.get('status') == ACCOUNT_STATUS_ACTIVE)
```

Просто у вас действительно было только 2 активных аккаунта, остальные были в `reserve`.
