# 🎨 РУКОВОДСТВО ПО ИНДИВИДУАЛЬНЫМ ВИТРИНАМ-КАНАЛАМ

## 📋 Описание

Функционал создания индивидуальных showcase-каналов для каждого аккаунта. Каждый аккаунт получает свой уникальный публичный канал, который можно добавить в витрину профиля.

---

## ✨ Основные возможности

### 1. Автоматическое создание канала
- Создаётся публичный канал (broadcast mode) в выбранном аккаунте
- Канал создаётся от имени конкретного аккаунта

### 2. Умная генерация username
- **Вариант 1**: базовый username (например, `showcase`)
- **Вариант 2**: базовый + номер аккаунта (например, `showcase1`)
- **Вариант 3**: базовый + "_" + номер (например, `showcase_1`)
- **Вариант 4**: базовый + случайный суффикс (например, `showcase_abc123`)

### 3. Автоматическая проверка доступности
- Проверяет каждый вариант username через Telegram API
- Если username занят → пробует следующий вариант
- Максимум 10 попыток

### 4. Автоматическое добавление в витрину профиля ⚡
- Использует `UpdatePersonalChannelRequest` из Telethon
- Канал **автоматически появляется** в витрине профиля
- Никаких ручных действий не требуется!

### 5. Сохранение информации
Все данные сохраняются в `bot_data.json`:
```json
{
  "accounts": {
    "+13434919340": {
      "showcase_channel": {
        "username": "showcase_1",
        "channel_id": 1234567890,
        "title": "Showcase Account 1",
        "created": "2026-01-21T10:30:00"
      }
    }
  }
}
```

---

## 🚀 Использование

### Команда `/createshowcase`

**Формат:**
```
/createshowcase <аккаунт> [базовый_username]
```

**Примеры:**

1. **По номеру аккаунта (1-10):**
   ```
   /createshowcase 1
   ```
   Создаст канал для аккаунта #1 с username `showcase`/`showcase1`/`showcase_1`

2. **По номеру телефона:**
   ```
   /createshowcase +13434919340
   ```
   Создаст канал для указанного номера

3. **С кастомным базовым username:**
   ```
   /createshowcase 1 vitrine
   ```
   Использует `vitrine` вместо `showcase`

---

## 📊 Что происходит при создании

1. **Проверка аккаунта**
   - Проверяется, что аккаунт существует
   - Проверяется авторизация аккаунта
   - Проверяется, нет ли уже showcase-канала

2. **Подключение к Telegram**
   - Создаётся клиент через Telethon
   - Используется сессия из `bot_data.json`
   - Учитывается proxy, если настроен

3. **Генерация и проверка username**
   - Генерируется список вариантов (10 штук)
   - Каждый проверяется через `get_entity()`
   - Выбирается первый свободный

4. **Создание канала**
   - Создаётся через `CreateChannelRequest`
   - Режим: `broadcast=True` (публичный канал)
   - Название: "Showcase {account_name}"

5. **Установка username**
   - Username устанавливается через `UpdateUsernameRequest`
   - Обрабатываются ошибки занятости

6. **Сохранение данных**
   - Информация о канале сохраняется в `bot_data.json`
   - Аккаунт помечается как имеющий showcase-канал

---

## ⚙️ Технические детали

### Функция `create_showcase_channel`

**Параметры:**
- `account_num`: номер аккаунта (1-10) или полный номер телефона
- `base_username`: базовый username для канала (по умолчанию `'showcase'`)

**Возвращает:**
- `success=True`: словарь с `username`, `channel_id`, `phone`, `title`
- `success=False`: строка с описанием ошибки

**Обработка ошибок:**
- `FloodWaitError`: слишком частые запросы
- `UsernameOccupiedError`: username внезапно стал занят
- `UsernameInvalidError`: некорректный username
- `ValueError`: канал не найден (означает, что username свободен - норма)
- `Exception`: прочие ошибки

### Алгоритм проверки username

```python
try:
    entity = await client.get_entity(variant)
    # Канал найден → username занят
    continue
except ValueError:
    # Канал не найден → username свободен!
    free_username = variant
    break
```

---

## 🎯 Добавление в витрину профиля

⚠️ **ВАЖНО**: Telegram API не предоставляет публичный метод для автоматического добавления канала в витрину профиля через MTProto.

### Ручное добавление:

1. **Откройте Telegram** на аккаунте, для которого создан канал
2. **Зайдите в настройки профиля**: Settings → Edit Profile
3. **Найдите раздел витрины** (Featured Channels / Add Channel)
4. **Нажмите "Add Channel"**
5. **Выберите созданный канал** из списка
6. **Подтвердите добавление**

---

## 📝 Примеры использования

### Создание витрин для нескольких аккаунтов

```bash
# Аккаунт 1
/createshowcase 1

# Аккаунт 2
/createshowcase 2

# Аккаунт 3
/createshowcase 3
```

### С разными базовыми username

```bash
# Для магазина
/createshowcase 1 shop

# Для услуг
/createshowcase 2 services

# Для новостей
/createshowcase 3 news
```

---

## 🔍 Проверка созданных каналов

### Через логи бота

```
✅ Showcase-канал создан: @showcase_1 (ID: 1234567890)
```

### Через bot_data.json

```json
{
  "accounts": {
    "+13434919340": {
      "name": "John",
      "showcase_channel": {
        "username": "showcase_1",
        "channel_id": 1234567890,
        "title": "Showcase John",
        "created": "2026-01-21T10:30:00"
      }
    }
  }
}
```

### Через Telegram

Перейдите по ссылке: `https://t.me/showcase_1`

---

## ⚠️ Ограничения и предупреждения

### 1. Один showcase-канал на аккаунт
- Каждый аккаунт может иметь только один showcase-канал
- При попытке создать второй - вернётся ошибка
- Решение: удалить существующий или использовать другой аккаунт

### 2. FloodWait защита
- Telegram ограничивает частоту создания каналов
- При превышении лимита - нужно подождать
- Типичное ожидание: 30-60 секунд

### 3. Username может быть занят
- Популярные username занимаются быстро
- Используйте уникальные базовые username
- Бот автоматически пробует альтернативы

### 4. Требуется авторизация
- Аккаунт должен быть авторизован через `/auth`
- Сессия должна быть активна
- При потере авторизации - повторите `/auth`

---

## 🛠️ Расширенные возможности

### Интеграция с существующими функциями

После создания showcase-канала можно использовать:

- `/set_profile_channel_avatar +1234` - установить аватар (работает для showcase)
- `/set_profile_channel_post +1234 Текст` - создать пост (работает для showcase)

⚠️ **Примечание**: Эти команды работают с `profile_channel`, а не `showcase_channel`. Для полной интеграции нужно расширить функционал.

---

## 📚 Связанные команды

- `/create_profile_channel` - создать обычный канал-витрину
- `/link_profile_channel` - привязать существующий канал
- `/list_profile_channels` - список всех каналов-витрин
- `/unlink_profile_channel` - отвязать канал

---

## 🐛 Решение проблем

### Ошибка "Аккаунт не авторизован"
**Решение:** Запустите `/auth +номер` для повторной авторизации

### Ошибка "Не удалось найти свободный username"
**Решение:** 
- Используйте другой базовый username
- Попробуйте позже (некоторые username могут освободиться)

### Ошибка "FloodWait"
**Решение:** Подождите указанное количество секунд и попробуйте снова

### Канал создан, но не отображается
**Решение:** 
- Проверьте через `https://t.me/username`
- Убедитесь, что username был установлен (проверьте логи)

---

## 📖 История изменений

### v1.0 (21 января 2026)
- ✅ Создание функции `create_showcase_channel`
- ✅ Добавление команды `/createshowcase`
- ✅ Автоматическая генерация username с проверкой доступности
- ✅ Сохранение информации в `bot_data.json`
- ✅ Обработка ошибок и исключений
- ✅ Документация и примеры использования

---

## 💡 Будущие улучшения

1. **Автоматическое добавление в витрину**
   - Исследовать возможность через Telegram API
   - Альтернатива: инструкции для пользователя

2. **Массовое создание**
   - Команда `/createshowcase_all` для всех аккаунтов
   - Настройка шаблонов username

3. **Управление showcase-каналами**
   - Команда `/list_showcases` - список всех showcase-каналов
   - Команда `/delete_showcase` - удалить showcase-канал

4. **Интеграция с profile_channel**
   - Объединение функционала showcase и profile_channel
   - Единые команды управления

---

## 📞 Поддержка

Если у вас возникли проблемы или вопросы:
- Проверьте логи: `bot_logs.txt`
- Изучите примеры в этом руководстве
- Запустите `/help` для списка всех команд
