# Руководство по диагностике воркеров

## Обзор изменений

Теперь система воркеров имеет **детальное логирование** и **автоматическое восстановление**.

## Что смотреть в логах

### 1. Старт воркеров

При запуске `/startmon` вы увидите:

```
================================================================================
🚀 CREATING 6 PARALLEL WORKERS
================================================================================
🔧 Creating worker #1/6 for [Account Name]
   Phone: +1234567890
   Status: active
   Session: ✅ EXISTS
   Will process: ALL 50 channels
   Offset: starts from channel #1
   ✅ Task created: worker_0_1234567890 (id=12345678)

🔧 Creating worker #2/6 for [Another Account]
   Phone: +0987654321
   Status: active
   Session: ✅ EXISTS
   Will process: ALL 50 channels
   Offset: starts from channel #2
   ✅ Task created: worker_1_0987654321 (id=87654321)
...
```

**Что проверить:**
- ✅ Создано нужное количество воркеров (6 из 6)
- ✅ У всех аккаунтов есть сессия (`Session: ✅ EXISTS`)
- ✅ Каждый воркер получил уникальный Task ID

### 2. Работа воркера

При запуске каждого воркера:

```
================================================================================
🚀 WORKER PROCESS STARTING
   Worker ID: worker_0_1234567890
   Account: Account Name (+1234567890)
   Status: ACTIVE
   Task ID: 123456789
   Index: 1/6
   Mode: CYCLIC
================================================================================
```

**Важно:** Если вы НЕ видите этих сообщений для всех 6 аккаунтов - значит воркеры не запустились!

### 3. Health Check (каждые 2 минуты)

```
🏥 Worker status check:
   Total tracked: 6
   Alive: 6
   Dead: 0
✅ Health check OK: 6/6 workers
```

**Если есть проблемы:**

```
🏥 Worker status check:
   Total tracked: 6
   Alive: 4
   Dead: 2

💀 Dead workers detected: 2
   worker_2_1234567890: USER_DEACTIVATED
   worker_3_0987654321: ConnectionError

🧹 Cleaning up 2 dead workers from tracking list
✅ Active workers list updated: 4 tasks remaining

⚠️ WORKER COUNT MISMATCH DETECTED!
   Expected: 6 workers
   Running: 4 workers
   Missing: 2 workers

🔄 Initiating automatic recovery...
```

### 4. Падение воркера

Если воркер упал с ошибкой:

```
================================================================================
💥 WORKER FATAL ERROR: worker_2_1234567890
   Account: TestAccount (+1234567890)
   Error: FloodWaitError: A wait of 3600 seconds is required
   Traceback:
     File "main.py", line 8567, in account_worker
       await client.send_message(...)
     ...
================================================================================
```

**И вы получите уведомление в Telegram:**
```
💥 ВОРКЕР УПАЛ

Аккаунт: TestAccount
Телефон: +1234567890
Ошибка: FloodWaitError: A wait of 3600 seconds is required

🔄 Система попытается восстановить через health check
```

### 5. Остановка воркера

```
================================================================================
🛑 WORKER STOPPING: worker_0_1234567890
   Account: TestAccount (+1234567890)
   Reason: Normal exit
================================================================================
```

**Причины остановки:**
- `Normal exit` - нормальное завершение (выполнены все циклы)
- `Monitoring stopped` - остановка через `/stopmon`
- Или конкретная ошибка, если воркер упал

## Команды для диагностики

### Проверить статус аккаунтов
```
/listaccounts
```
Показывает все аккаунты и их статусы (`active`, `reserve`, `broken`)

### Проверить количество воркеров
Смотрите в логах последнее сообщение:
```
✅ Health check OK: N/M workers
```
Где:
- `N` = количество работающих воркеров
- `M` = ожидаемое количество (= количество активных аккаунтов)

### Если воркеров меньше, чем нужно

**Причина 1:** Нет активных аккаунтов
```
/listaccounts
/toggleaccount +1234567890  # Активировать аккаунт
```

**Причина 2:** Лимит параллельных воркеров
```
/setparallel 6  # Увеличить до 6 воркеров
```

**Причина 3:** У аккаунта нет сессии
Смотрите в логах:
```
Session: ❌ MISSING
```
Решение: переавторизовать аккаунт через `/auth`

## Типичные проблемы и решения

### Проблема: "Работает только 1 воркер"

**Диагностика:**
1. Проверьте логи при старте - сколько воркеров создано?
2. Проверьте `/listaccounts` - сколько аккаунтов в статусе `active`?
3. Проверьте `/setparallel` - не стоит ли лимит = 1?

**Решения:**
```bash
# Активировать больше аккаунтов
/toggleaccount +1234567890
/toggleaccount +0987654321

# Увеличить лимит параллельных воркеров
/setparallel 6
```

### Проблема: "Воркеры постоянно падают"

**Смотрите в логах причину:**

**FloodWait:**
```
💥 WORKER FATAL ERROR: ...
Error: FloodWaitError: A wait of 3600 seconds is required
```
Решение: Уменьшить скорость комментирования

**USER_DEACTIVATED:**
```
💥 WORKER FATAL ERROR: ...
Error: USER_DEACTIVATED
```
Решение: Аккаунт забанен, нужен новый

**ConnectionError:**
```
💥 WORKER FATAL ERROR: ...
Error: ConnectionError
```
Решение: Проблемы с сетью, перезапустить бота

### Проблема: "Health check показывает мертвые воркеры"

Это **нормально** - система автоматически:
1. Обнаруживает мертвые воркеры
2. Очищает их из списка
3. Запускает автовосстановление

Просто дождитесь сообщения:
```
🔄 Initiating automatic recovery...
✅ Monitoring restarted successfully
```

## Мониторинг в реальном времени

### Следить за логами
```bash
tail -f bot_logs.txt | grep -E "(WORKER|Health check|🔧|💥|🛑)"
```

Вы увидите:
- 🔧 = создание воркеров
- 🚀 = старт воркера
- 💥 = падение воркера
- 🛑 = остановка воркера
- 🏥 = health check

### Проверить живые воркеры прямо сейчас

В логах найдите последний:
```
🏥 Worker status check:
   Total tracked: 6
   Alive: 6
   Dead: 0
```

Это показывает **текущее состояние** воркеров.

## Рекомендации

1. **Всегда проверяйте логи** при запуске `/startmon`
2. **Следите за Health check** - он срабатывает каждые 2 минуты
3. **При падении воркера** смотрите полную трассировку в логах
4. **Используйте тестовый режим** для проверки новых аккаунтов:
   ```
   /testmode on @test_channel
   ```

## Автоматическое восстановление

Система **автоматически**:
- ✅ Обнаруживает упавшие воркеры (каждые 2 минуты)
- ✅ Очищает мертвые таски из списка
- ✅ Перезапускает мониторинг с правильным количеством воркеров
- ✅ Уведомляет владельца о проблемах

Вам нужно только:
- Следить за уведомлениями в Telegram
- При необходимости добавлять новые аккаунты
- Регулировать скорость комментирования

## Поддержка

Если проблемы продолжаются:
1. Соберите логи за последние 10 минут
2. Отправьте фрагмент с ошибками
3. Укажите, какие команды выполняли
