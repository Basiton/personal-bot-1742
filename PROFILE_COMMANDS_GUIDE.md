# Команды управления профилями аккаунтов

## Новые команды для управления профилями

### `/setname <Имя Фамилия>`
Изменяет имя профиля для **всех активных** авторизованных аккаунтов.

**Примеры:**
```
/setname Иван Петров
/setname Мария
```

**Что делает:**
- Обновляет имя и фамилию для всех активных аккаунтов
- Сохраняет изменения в базу данных с timestamp
- Показывает результат для каждого аккаунта (✅ успех / ❌ ошибка)
- Логирует старое и новое значение

**Обработка ошибок:**
- ❌ Нет активных аккаунтов
- ❌ Аккаунт не авторизован
- ❌ Сетевые ошибки (timeout, proxy)
- ❌ Ошибки Telegram API (FloodWait, etc.)

---

### `/setbio <Новое описание>`
Изменяет био/описание профиля для **всех активных** авторизованных аккаунтов.

**Примеры:**
```
/setbio Люблю программирование и путешествия 🌍
/setbio Инвестор | Трейдер | t.me/mychannel
```

**Что делает:**
- Обновляет раздел "О себе" для всех активных аккаунтов
- Сохраняет изменения в базу данных с timestamp
- Показывает результат для каждого аккаунта
- Логирует старое и новое значение

**Обработка ошибок:**
- ❌ Нет активных аккаунтов
- ❌ Аккаунт не авторизован
- ❌ Сетевые ошибки
- ❌ Ошибки Telegram API

---

### `/setavatar`
Загружает аватарку для **всех активных** авторизованных аккаунтов.

**Как использовать:**
1. Отправить команду `/setavatar`
2. Бот ответит с инструкцией
3. Отправить фото (jpg, png) в ответ на сообщение бота
4. Бот загрузит аватарку для всех активных аккаунтов

**Примеры:**
```
/setavatar
[Отправить фото]
```

**Что делает:**
- Устанавливает состояние ожидания фото
- Принимает фото от пользователя
- Загружает фото как аватарку для всех активных аккаунтов
- Сохраняет результат в базу данных
- Удаляет временный файл после загрузки
- Показывает результат для каждого аккаунта

**Обработка ошибок:**
- ❌ Нет активных аккаунтов
- ❌ Ошибка загрузки фото
- ❌ Аккаунт не авторизован
- ❌ Ошибки при загрузке на Telegram

**Timeout:**
- ⏱️ 5 минут на отправку фото (после этого состояние автоматически сбрасывается)

---

### `/profile`
Показывает текущую информацию о профилях всех активных аккаунтов.

**Примеры:**
```
/profile
```

**Что показывает:**
- 📱 Последние 4 цифры телефона
- 👤 Имя и фамилия
- 🔗 Username (если есть)
- 📝 Био (первые 50 символов)
- ✅ Статус авторизации

**Пример вывода:**
```
👥 ПРОФИЛИ АКТИВНЫХ АККАУНТОВ

Всего аккаунтов: 3

📱 1234
👤 Иван Петров
🔗 @ivan_petrov
📝 Люблю программирование и путешествия 🌍
✅ Статус: Активен

📱 5678
👤 Мария Сидорова
📝 Инвестор | Трейдер
✅ Статус: Активен

... и еще 1 аккаунт

💡 Используйте:
/setname - изменить имя
/setbio - изменить описание
/setavatar - загрузить аватар
```

**Обработка ошибок:**
- ❌ Нет активных аккаунтов
- ❌ Ошибка подключения к аккаунту

---

## База данных

### Таблица `profile_changes`
Хранит историю всех изменений профиля.

**Структура:**
```sql
CREATE TABLE profile_changes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    phone TEXT,                    -- Телефон аккаунта
    change_type TEXT,              -- Тип изменения: 'name', 'bio', 'avatar'
    old_value TEXT,                -- Старое значение
    new_value TEXT,                -- Новое значение
    change_date TEXT,              -- Дата и время изменения (ISO format)
    success INTEGER DEFAULT 1      -- 1 = успех, 0 = ошибка
)
```

**Примеры записей:**
```
| phone        | change_type | old_value    | new_value    | change_date          | success |
|--------------|-------------|--------------|--------------|----------------------|---------|
| +79123456789 | name        | John Doe     | Иван Петров  | 2026-01-14T10:30:00  | 1       |
| +79123456789 | bio         | Hello world  | Люблю код    | 2026-01-14T10:31:00  | 1       |
| +79123456789 | avatar      |              | uploaded     | 2026-01-14T10:32:00  | 1       |
```

---

## Использование с Telethon API

### UpdateProfileRequest
```python
from telethon.tl.functions.account import UpdateProfileRequest

await client(UpdateProfileRequest(
    first_name="Иван",
    last_name="Петров",
    about="Описание профиля"
))
```

### UploadProfilePhotoRequest
```python
from telethon.tl.functions.photos import UploadProfilePhotoRequest

await client(UploadProfilePhotoRequest(
    file=await client.upload_file("/path/to/photo.jpg")
))
```

---

## Безопасность и лимиты

### Рекомендации:
1. **Не меняйте профиль слишком часто** - это может вызвать подозрения у Telegram
2. **Используйте естественные данные** - реалистичные имена и био
3. **Задержки между изменениями** - подождите 5-10 минут между командами
4. **Разные данные для разных аккаунтов** - не используйте одинаковые имена

### Лимиты Telegram:
- Имя: до 64 символов (first_name + last_name)
- Био: до 70 символов
- Аватар: jpg, png (рекомендуется до 5 МБ)

### Обработка FloodWait:
Если Telegram вернет FloodWait ошибку, команда покажет:
```
❌ 1234: FloodWait: retry after 300 seconds
```

---

## Примеры сценариев использования

### Сценарий 1: Обновление всех аккаунтов
```
1. /setname Александр Иванов
2. Подождать 2 минуты
3. /setbio Инвестор | Трейдер | Крипто-энтузиаст
4. Подождать 2 минуты
5. /setavatar
6. [Отправить фото]
7. /profile (проверить результат)
```

### Сценарий 2: Проверка профилей перед стартом
```
1. /profile
2. Проверить, что все аккаунты активны
3. При необходимости обновить данные
4. /startmon
```

### Сценарий 3: Массовое обновление био со ссылкой
```
1. /setbio Подписывайтесь на мой канал: t.me/mychannel
2. Проверить результат в /profile
3. При ошибках посмотреть логи
```

---

## Troubleshooting

### Ошибка: "Нет активных аккаунтов"
**Решение:**
```
/activeaccounts - проверить активные
/toggleaccount +79123456789 - активировать нужный
```

### Ошибка: "Аккаунт не авторизован"
**Решение:**
```
/auth +79123456789 [proxy] - переавторизовать
```

### Ошибка: "FloodWait"
**Решение:**
- Подождать указанное время
- Не спамить командами
- Использовать задержки между операциями

### Фото не загружается
**Решение:**
- Проверить формат (jpg, png)
- Проверить размер (< 10 МБ)
- Отправить заново
- Проверить права доступа к /tmp/

---

## FAQ

**Q: Команды работают только для активных аккаунтов?**  
A: Да, все команды (`/setname`, `/setbio`, `/setavatar`) работают только для аккаунтов с `active=True`.

**Q: Можно ли установить разные данные для разных аккаунтов?**  
A: Текущие команды устанавливают одинаковые данные для всех. Для индивидуальной настройки используйте `/accounts`.

**Q: Сохраняется ли история изменений?**  
A: Да, все изменения сохраняются в таблицу `profile_changes` с timestamp.

**Q: Что делать, если команда зависла?**  
A: Подождите 30 секунд. Если не помогло, используйте `/stopmon` и перезапустите бота.

**Q: Можно ли отменить /setavatar после отправки команды?**  
A: Просто не отправляйте фото в течение 5 минут, состояние автоматически сбросится.

---

## Структура кода

### Основные функции:
- `log_profile_change()` - логирование изменений в БД
- `setname_command()` - обработчик команды /setname
- `setbio_command()` - обработчик команды /setbio
- `setavatar_command()` - обработчик команды /setavatar
- `profile_command()` - обработчик команды /profile
- `handle_avatar_photo()` - обработчик загрузки фото

### Telethon API:
- `UpdateProfileRequest` - изменение имени и био
- `UploadProfilePhotoRequest` - загрузка аватарки
- `client.upload_file()` - загрузка файла на сервер
- `client.get_me()` - получение информации о текущем пользователе
