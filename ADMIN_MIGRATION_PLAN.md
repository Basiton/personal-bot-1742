# 🔧 План миграции: Разделение контекстов админов

## Статус: В процессе реализации

### ✅ Выполнено:

1. **Добавлены константы супер-админов**
   - `SUPER_ADMINS = [6730216440, 5912533270]`

2. **Добавлены методы проверки прав**
   - `is_super_admin(user_id)` - проверка супер-админа
   - `is_admin(user_id)` - проверка любого админа
   - `get_admin_id(user_id)` - получение admin_id для фильтрации

3. **Расширена структура БД**
   - Добавлено поле `admin_id INTEGER DEFAULT NULL` во все таблицы:
     - blocked_accounts
     - comment_history
     - parsed_channels
     - blocked_channels
     - profile_changes
     - account_stats
   - Добавлена автоматическая миграция существующих таблиц

### 🔄 В процессе:

4. **Модификация сохранения/загрузки данных**
   - accounts_data должен содержать admin_id для каждого аккаунта
   - channels должны быть привязаны к admin_id
   - Нужно обновить save_data() и load_data()

5. **Обновление команд с фильтрацией по admin_id**
   Команды которые нужно обновить:
   - `/stats` - фильтровать по admin_id
   - `/listaccounts` - показывать только свои аккаунты
   - `/auth` - привязывать аккаунт к admin_id
   - `/addchannel` - привязывать канал к admin_id
   - `/testmode` - работать только со своими каналами
   - все остальные команды работы с аккаунтами

6. **Новые команды для супер-админов**
   - `/stats_global` - глобальная статистика
   - `/stats_admin <admin_id>` - статистика конкретного админа
   - `/listadmins` - список всех админов
   - Модификация `/addadmin` - только для супер-админов

### 📋 Следующие шаги:

1. Создать структуру для хранения информации об админах (username, date_added, etc)
2. Обновить методы add_comment_stat() и другие для сохранения admin_id
3. Модифицировать команду `/stats` с фильтрацией
4. Реализовать новые команды для супер-админов
5. Обновить все команды управления аккаунтами
6. Создать миграционный скрипт для существующих данных
7. Тестирование

### ⚠️ Важные замечания:

- Существующие данные без admin_id будут иметь NULL
- Супер-админы получат доступ ко всем данным (admin_id = NULL в фильтрах)
- Обычные админы будут видеть только данные со своим admin_id
- При добавлении нового админа создаётся отдельное пространство

### 🔧 Технические детали:

**Фильтрация данных:**
```python
# Для супер-админа: WHERE admin_id IS NULL OR admin_id IN (all_admins)
# Для обычного админа: WHERE admin_id = user_id
```

**Структура accounts_data:**
```python
{
  "+phone": {
    "session": "...",
    "name": "...",
    "status": "active",
    "admin_id": 123456789  # NEW
  }
}
```

**Структура admin_info (NEW):**
```python
{
  123456789: {
    "username": "@user",
    "added_date": "2026-01-17",
    "added_by": 6730216440
  }
}
```
