# 🚀 Быстрый старт: Тестирование на @NFT_Giveaway_TG и @AIGIRLSARTS

## Шаг 1: Добавьте каналы (если ещё не добавлены)

```bash
/addchannel @NFT_Giveaway_TG
/addchannel @AIGIRLSARTS
```

## Шаг 2: Включите тестовый режим

```bash
/testmode on @NFT_Giveaway_TG @AIGIRLSARTS
```

Ответ бота:
```
🧪 ТЕСТОВЫЙ РЕЖИМ ВКЛЮЧЕН

✅ Бот будет комментировать ТОЛЬКО:
  • @NFT_Giveaway_TG
  • @AIGIRLSARTS

⚡ Лимит скорости: 10 комм/час на аккаунт

⚠️ ВСЕ ОСТАЛЬНЫЕ КАНАЛЫ ИГНОРИРУЮТСЯ!

💡 Для выключения: /testmode off
```

## Шаг 3: (Опционально) Установите низкую скорость для наблюдения

```bash
/testmode speed 5
```

Это установит лимит 5 комментариев в час на аккаунт.

## Шаг 4: Запустите бота

```bash
/startmon
```

## Шаг 5: Наблюдайте логи

В терминале/логах вы увидите:
```
🧪 TEST MODE ACTIVE: Using 2 test channels: ['@NFT_Giveaway_TG', '@AIGIRLSARTS']
🧪 Speed limit: 5 msg/hour per account
🧪 ⚠️ ALL OTHER CHANNELS WILL BE IGNORED!

🧪 TEST MODE SUCCESS:
   Channel: @NFT_Giveaway_TG
   Account: John Smith (+18622376920)
   Comment: Интересный проект! Отличная возможность для...
   Time: 14:35:22
   Post ID: 12345
```

## Шаг 6: Проверяйте качество

### Проверка 1: Логи бота ✅
- Читайте текст комментариев в логах
- Убедитесь, что они релевантны теме канала
- Проверьте отсутствие дубликатов

### Проверка 2: Telegram ✅
- Откройте каналы @NFT_Giveaway_TG и @AIGIRLSARTS в Telegram
- Найдите комментарии от ваших аккаунтов
- Проверьте:
  - Естественность текста
  - Отсутствие спама
  - Правильное форматирование
  - Нет повторяющихся комментариев

### Проверка 3: Интервалы ✅
- Убедитесь, что комментарии не идут подряд от одного аккаунта
- Проверьте соблюдение интервалов
- Нет флуда

## Шаг 7: Остановите и выключите тестовый режим

После проверки (30-60 минут):

```bash
# Остановить бота
/stopmon

# Выключить тестовый режим
/testmode off
```

Ответ бота:
```
🔴 ТЕСТОВЫЙ РЕЖИМ ВЫКЛЮЧЕН

✅ Бот вернулся к обычной работе со всеми каналами
```

## Что делать, если обнаружены проблемы

### Проблема: Нерелевантные комментарии
**Решение:** 
- Проверьте шаблоны: `/listtemplates`
- Добавьте более качественные шаблоны: `/addtemplate`
- Настройте Yandex GPT (см. YANDEX_GPT_GUIDE.md)

### Проблема: Дубликаты комментариев
**Решение:**
- В тестовом режиме это уже предотвращается автоматически
- Если всё равно видите дубликаты, проверьте логи на предупреждения
- Добавьте больше шаблонов для разнообразия

### Проблема: Слишком частые комментарии
**Решение:**
```bash
/testmode speed 3
```
Снизьте скорость до 3 комм/час

### Проблема: FloodWait ошибки
**Решение:**
- Это нормально при высокой активности
- Бот автоматически ждёт необходимое время
- Снизьте скорость или уменьшите количество активных аккаунтов

## Полезные команды во время теста

### Проверить статус
```bash
/testmode
```

### Проверить статистику
```bash
/stats
```

### Проверить активные аккаунты
```bash
/activeaccounts
```

### Проверить последние комментарии
```bash
/history
```

## Готово к продакшену?

Если после тестирования всё работает хорошо:

1. **Выключите тестовый режим**
   ```bash
   /testmode off
   ```

2. **Установите нормальную скорость** (если меняли)
   ```bash
   /setratelimit 20
   ```

3. **Добавьте больше каналов**
   ```bash
   /addchannel @channel1
   /addchannel @channel2
   # и т.д.
   ```

4. **Запустите бота**
   ```bash
   /startmon
   ```

Бот теперь будет работать со всеми каналами в обычном режиме!

## Повторное тестирование

В любой момент можно вернуться к тестовому режиму:

```bash
# Для проверки новых каналов
/testmode on @NewChannel1 @NewChannel2
/startmon

# После проверки
/stopmon
/testmode off
```

## Заметки

- ✅ Тестовый режим не влияет на сохранённые настройки
- ✅ При выключении всё возвращается к норме
- ✅ Можно включать/выключать сколько угодно раз
- ✅ Отлично подходит для проверки новых каналов или настроек

---

**💡 Совет:** Всегда начинайте с тестового режима при добавлении новых каналов или изменении настроек!
