# 🔄 СХЕМА РАБОТЫ КОМАНДЫ /showcase

```
┌─────────────────────────────────────────────────────────────┐
│                   ПОЛЬЗОВАТЕЛЬ ВВОДИТ                       │
│                     /showcase <args>                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              ОБРАБОТЧИК showcase_command()                  │
│           pattern: ^/showcase(?:\s|$)                       │
│                                                             │
│  1. Проверка прав админа                                   │
│  2. Логирование: "команда получена от admin X"             │
│  3. Парсинг текста команды                                 │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
                 ┌───────┴────────┐
                 │  Есть args?    │
                 └───────┬────────┘
                    Нет  │  Да
                    ▼────┘  ▼
              ┌─────────────────────────┐
              │  Показать справку       │
              └─────────────────────────┘
                         │
                         ▼
        ┌────────────────┴────────────────┐
        │  Извлечь action из args         │
        │  Логирование: "подкоманда: X"   │
        └────────────────┬────────────────┘
                         │
        ┌────────────────┴────────────────────────────┐
        │                                             │
   ┌────▼────┐  ┌────▼────┐  ┌────▼────┐  ┌────▼────┐
   │ create  │  │  link   │  │ unlink  │  │  list   │
   └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
        │            │            │            │
        ▼            ▼            ▼            ▼
┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐
│_showcase_ │ │_showcase_ │ │_showcase_ │ │_showcase_ │
│  create() │ │  link()   │ │ unlink()  │ │  list()   │
└───────────┘ └───────────┘ └───────────┘ └───────────┘
        │
        │
   ┌────▼────┐  ┌────▼────┐
   │  info   │  │   set   │
   └────┬────┘  └────┬────┘
        │            │
        ▼            ▼
┌───────────┐ ┌───────────────────────────────────┐
│_showcase_ │ │      _showcase_set()              │
│  info()   │ │                                   │
└───────────┘ │  ┌─────────────────────────────┐  │
              │  │ Параметры:                  │  │
              │  │ • avatar                    │  │
              │  │ • title                     │  │
              │  │ • about                     │  │
              │  │ • info (title:X|about:Y) ✨ │  │
              │  │ • post                      │  │
              │  │ • post_pin                  │  │
              │  └─────────────────────────────┘  │
              └───────────────────────────────────┘

```

## 🔍 ПОДРОБНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ

### 1. Вход: Команда от пользователя
```
/showcase set +1 info title:Test|about:Description
```

### 2. Обработчик showcase_command()
```python
@self.bot_client.on(events.NewMessage(pattern=r'^/showcase(?:\s|$)'))
async def showcase_command(event):
    logger.info(f"🎨 /showcase команда получена от admin {event.sender_id}: {event.text}")
```

**Лог:**
```
🎨 /showcase команда получена от admin 123456: /showcase set +1 info title:Test|about:Description
```

### 3. Парсинг команды
```python
parts = event.text.split(maxsplit=1)
# parts = ["/showcase", "set +1 info title:Test|about:Description"]

args = parts[1].split(maxsplit=1)
# args = ["set", "+1 info title:Test|about:Description"]

action = args[0].lower()
# action = "set"

logger.info(f"🎨 /showcase подкоманда: {action}")
```

**Лог:**
```
🎨 /showcase подкоманда: set
```

### 4. Маршрутизация к _showcase_set()
```python
if action == "set":
    await self._showcase_set(event, args[1])
    # args[1] = "+1 info title:Test|about:Description"
```

**Лог:**
```
📺 /showcase set: инициирован admin 123456, args=+1 info title:Test|about:Description
```

### 5. Обработка в _showcase_set()
```python
parts = args_str.split(maxsplit=2)
# parts = ["+1", "info", "title:Test|about:Description"]

phone = parts[0]     # "+1"
param = parts[1]     # "info"
value = parts[2]     # "title:Test|about:Description"
```

### 6. Обработка параметра "info"
```python
if param == "info":
    logger.info(f"📺 Парсинг info параметров: {value}")
    
    pairs = value.split('|')
    # pairs = ["title:Test", "about:Description"]
    
    for pair in pairs:
        key, val = pair.split(':', 1)
        # key="title", val="Test"
        # key="about", val="Description"
```

**Лог:**
```
📺 Парсинг info параметров: title:Test|about:Description
📺 Разделено на пары: ['title:Test', 'about:Description']
📺 Извлечено: title = Test
📺 Извлечено: about = Description
📺 Вызов update_profile_channel_info: phone=+1, title=Test, about=Description
```

### 7. Обновление канала
```python
success, message = await self.update_profile_channel_info(
    phone, 
    title=title, 
    about=about
)
```

### 8. Результат пользователю
```
✅ ИНФОРМАЦИЯ ОБНОВЛЕНА

📝 Название: Test
📄 Описание: Description
```

**Лог:**
```
📺 /showcase set info: успешно обновлено для +1 (admin 123456)
```

## 📊 ТАБЛИЦА МАРШРУТИЗАЦИИ

| Команда | Action | Функция | Что делает |
|---------|--------|---------|------------|
| `/showcase create +1 Name` | `create` | `_showcase_create()` | Создаёт новый канал |
| `/showcase link +1 @ch` | `link` | `_showcase_link()` | Привязывает канал |
| `/showcase unlink +1` | `unlink` | `_showcase_unlink()` | Отвязывает канал |
| `/showcase list` | `list` | `_showcase_list()` | Список витрин |
| `/showcase info +1` | `info` | `_showcase_info()` | Инфо о витрине |
| `/showcase set +1 ...` | `set` | `_showcase_set()` | Настройка витрины |

## 🎯 ОСОБЕННОСТИ ПАТТЕРНА

### Паттерн: `^/showcase(?:\s|$)`

**Что означает:**
- `^` - начало строки
- `/showcase` - буквально "/showcase"
- `(?:\s|$)` - за ним ОБЯЗАТЕЛЬНО пробел `\s` ИЛИ конец строки `$`

**Примеры:**
- ✅ `/showcase` (конец строки)
- ✅ `/showcase list` (пробел после)
- ✅ `/showcase set +1 title Test` (пробел после)
- ❌ `/createshowcase` (нет пробела, не конец строки)
- ❌ `/showcase_test` (подчёркивание, не пробел)

## 🚀 ПРЕИМУЩЕСТВА НОВОЙ СХЕМЫ

✅ **Централизованная обработка**
- Один вход для всех команд
- Единообразное логирование
- Консистентная валидация прав

✅ **Модульность**
- Каждая подкоманда в отдельной функции
- Легко добавлять новые команды
- Легко тестировать

✅ **Отладка**
- Подробное логирование на каждом этапе
- Понятные сообщения об ошибках
- Трассировка выполнения

✅ **Масштабируемость**
- Легко добавить новые подкоманды
- Простая маршрутизация
- Понятная структура кода
