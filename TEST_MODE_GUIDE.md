# 🧪 Тестовый режим бота

## Обзор

Тестовый режим позволяет запустить бота только на выбранных каналах с пониженной скоростью для проверки качества комментариев, релевантности и отсутствия ошибок.

## Возможности

### 1. Работа только с выбранными каналами
- Бот комментирует **только** указанные каналы
- Все остальные каналы игнорируются
- Идеально для проверки работы на 1-2 каналах

### 2. Пониженная скорость
- Отдельный лимит скорости для тестового режима (по умолчанию 10 комм/час)
- Не влияет на основной лимит скорости
- Можно настроить от 1 до 30 комм/час

### 3. Детальное логирование
В тестовом режиме каждый комментарий логируется с подробной информацией:
- 📢 Канал
- 👤 Аккаунт (имя и телефон)
- 💬 Текст комментария (первые 50 символов)
- 🕐 Время отправки
- 📋 ID поста

### 4. Защита от дубликатов
- Автоматическая проверка последних 10 комментариев
- Если обнаружен дубликат, комментарий регенерируется
- Логирование всех случаев дубликатов

### 5. Детальное логирование ошибок
При ошибках в тестовом режиме логируется:
- 📢 Канал
- 👤 Аккаунт
- ❌ Текст ошибки
- 🕐 Время

## Команды

### Проверить статус
```
/testmode
```
Показывает:
- Включён ли тестовый режим
- Список тестовых каналов
- Текущий лимит скорости

### Включить тестовый режим
```
/testmode on @NFT_Giveaway_TG @AIGIRLSARTS
```
- Включает тестовый режим
- Устанавливает список тестовых каналов
- Бот начнёт работать только с этими каналами

### Выключить тестовый режим
```
/testmode off
```
- Выключает тестовый режим
- Бот возвращается к работе со всеми каналами

### Установить скорость
```
/testmode speed 5
```
- Устанавливает лимит скорости для тестового режима
- Диапазон: 1-30 комм/час на аккаунт
- По умолчанию: 10 комм/час

## Примеры использования

### Пример 1: Тест на двух каналах
```bash
# Включить тестовый режим
/testmode on @NFT_Giveaway_TG @AIGIRLSARTS

# Установить низкую скорость для наблюдения
/testmode speed 5

# Запустить мониторинг
/startmon

# Наблюдать логи...

# Остановить после проверки
/stopmon

# Выключить тестовый режим
/testmode off
```

### Пример 2: Проверка одного канала
```bash
# Тест только одного канала
/testmode on @TestChannel

# Запустить
/startmon

# После проверки выключить
/testmode off
```

## Что проверять

### ✅ Корректность логики комментинга
- Комментарии появляются в правильных каналах
- Соблюдается интервал между комментариями
- Нет комментариев подряд от одного аккаунта

### ✅ Релевантность ответов
- Комментарии соответствуют теме канала
- Комментарии выглядят естественно
- Нет явно неуместных комментариев

### ✅ Отсутствие «рассыпания»
- Нет дубликатов комментариев подряд
- Нет повторений одного и того же текста
- Правильное форматирование

### ✅ Обработка ошибок
- При FloodWait бот корректно ждёт
- При блокировке канала бот не ломается
- Все ошибки логируются

## Логи в тестовом режиме

### Успешный комментарий
```
🧪 TEST MODE SUCCESS:
   Channel: @NFT_Giveaway_TG
   Account: John Smith (+18622376920)
   Comment: Интересный проект! Отличная возможность для...
   Time: 14:35:22
   Post ID: 12345
```

### Ошибка
```
🧪 TEST MODE ERROR:
   Channel: @AIGIRLSARTS
   Account: Jane Doe (+12539061764)
   Error: FloodWaitError: A wait of 120 seconds is required
   Time: 14:36:15
```

### Обнаружен дубликат
```
🧪 TEST MODE: Duplicate comment detected! Regenerating...
```

## Технические детали

### Как работает фильтрация каналов
1. При `test_mode = True` создаётся список `channels_to_use`
2. Из основного списка каналов отбираются только те, что в `test_channels`
3. Только эти каналы передаются в воркеры аккаунтов

### Как работает лимит скорости
- В методе `can_account_send_message()` проверяется флаг `test_mode`
- Если включён, используется `test_mode_speed_limit` вместо `messages_per_hour`
- Основной лимит не изменяется

### Проверка дубликатов
- Хранятся последние 10 комментариев в `_last_test_comments`
- Перед отправкой проверяется наличие в списке
- Если найден, комментарий регенерируется
- Работает только в тестовом режиме

## Рекомендации

### 🔹 Начните с малого
- Включите режим на 1-2 каналах
- Установите низкую скорость (5-7 комм/час)
- Наблюдайте логи в течение 30-60 минут

### 🔹 Проверяйте качество
- Читайте логи и смотрите на текст комментариев
- Проверяйте каналы визуально в Telegram
- Убедитесь, что комментарии уместны

### 🔹 Постепенно увеличивайте
- Если всё работает хорошо, увеличьте скорость
- Добавьте больше каналов
- Когда уверены, выключите тестовый режим

### 🔹 Используйте для отладки
- При обнаружении проблем включите тестовый режим
- Воспроизведите проблему на конкретных каналах
- Изучите детальные логи

## Безопасность

### ⚠️ Тестовый режим НЕ отменяет
- Защиту от флуда (интервалы между комментариями)
- Проверку блокировок аккаунтов
- Базовые правила безопасности

### ✅ Тестовый режим добавляет
- Дополнительную проверку дубликатов
- Детальное логирование для анализа
- Контролируемую нагрузку

## Troubleshooting

### Бот не комментирует в тестовых каналах
**Проблема:** После включения `/testmode on @channel` бот не работает

**Решение:**
1. Проверьте, что каналы есть в основном списке: `/listchannels`
2. Убедитесь, что используете правильный формат: `@channel` (с собакой)
3. Проверьте логи: должна быть строка "TEST MODE ACTIVE"

### Каналы не найдены
**Проблема:** Ошибка "None of test channels found in channels list"

**Решение:**
1. Добавьте канал в основной список: `/addchannel @channel`
2. Проверьте формат (с @ или без)
3. Проверьте, что канал не заблокирован

### Слишком медленно
**Проблема:** Комментарии отправляются очень редко

**Решение:**
1. Увеличьте скорость: `/testmode speed 15`
2. Убедитесь, что в каналах есть новые посты
3. Проверьте количество активных аккаунтов

## Вывод

Тестовый режим — это мощный инструмент для:
- ✅ Проверки качества работы бота
- ✅ Отладки проблем
- ✅ Безопасного тестирования новых настроек
- ✅ Контролируемого запуска на новых каналах

Используйте его всегда, когда вносите изменения или добавляете новые каналы!
